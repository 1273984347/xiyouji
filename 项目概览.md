# 详解西游记 · 结构化项目概览

> 提取时间：2026-08-10 · 当前 HEAD = **v2.3.33 · W418**（已 commit，下一编号 W419）。
> 来源：交接文档.md / README.md / STRUCTURE.md / DESIGN.md / 项目认知总览.md / 项目交接参考手册.md / docs/00-导读/项目说明.md + git 实测。
> 计数说明：各文档间计数存在 +1~+12 漂移（文档滞后），本文锚定 `verify_delivery.py` 硬门禁值，并以"约/实测"标注；真值以脚本为准。

---

## 一、项目背景缘起

| 项 | 内容 |
|---|---|
| 项目名 | 详解西游记（xiyouji） |
| 创建日期 | 2026-07-21（单人项目，GitHub `1273984347`） |
| 协作模式 | 「真人 + AI 代理」协作：主代理派发 + subagent 并行产出 + 主代理 spot-check 复核；以 `W###` 编号串起双索引 |
| 核心理念 | **「一源多形」**：把《西游记》解读组织为四层——原文本 → Markdown 解读 → 可浏览 HTML 站点 → Python 数据分析 |
| 写作动机 | 每一代人有自己的西游（明人讽喻/清人证道/20世纪革命/21世纪职场）；项目把"重读痕迹、反复母题、被争论四百年的问题"安顿下来，让下一个读者不必从零开始 |
| 当前定位 | 已是"静态博物馆"——内容模板化扩容边际收益趋零，**战略重心转向工程化（RAG 真实生成 / 数据 API / 图谱 / 发布 / 英文站），而非继续加静态篇目** |

---

## 二、业务目标

四条产品目标（四可）：

- **可读**：每篇独立成章，不同轨别（大众普及 / 学术研究 / 教学讲解 / 个人创作）服务不同读者。
- **可查**：人物 / 回目 / 主题互链，全站搜索浮层 + 数据 API 检索。
- **可扩**：目录与命名稳定（A–AH 34 类脚本 / `第NNN回-*.md` / kebab-case.html）。
- **可算**：原著可被脚本处理，产出词频 / 共现网络 / 情感分析 / 难度系数等。

目标读者分层：大众读者、学术研究者、教学者、同好创作者、数据可视化爱好者、跨学科研究者、批评史研究者、全年龄段读者。

---

## 三、技术栈选型

| 层 | 选型 | 关键决策 |
|---|---|---|
| 前端 | 纯静态 HTML + **D3.js v7 CDN** + Three.js（3D 页） | **零依赖、`file://` 可直接打开**；数据内嵌 `EMBEDDED_DATA` fallback，禁 fetch 跨域 JSON |
| 设计系统 | `tokens.css` + `system.css`（新中式·数字雅集，W334 重设计） | 27 个 CSS 变量集中 `:root`，禁裸色值；Hero 渐变固定；宋体/等宽字体栈 + 子集化 woff2 |
| 后端（可选） | Python **stdlib** `http.server`（零第三方依赖） | 数据 API（8787）、RAG 后端（8777）均为零依赖 |
| 文本分析 | Python（jieba/Pillow，按 A–AH 34 类组织 130+ 脚本） | 词频 / 共现网络 / 情感分析 / NLP pipeline（BookNLP 零依赖落地） |
| RAG | 自研零依赖 BM25 + 图谱层；`LLM_API_KEY` 存在时走真实生成（W402 接通） | provider 化 Base URL（7 厂商 + 代理网关），OpenAI 兼容 / Anthropic 双格式适配器 |
| 测试/CI | Playwright E2E 三层 + pytest + GitHub Actions（5 workflow） | 全 CI 已转绿（CI/security/deploy） |
| Agent 平行入口 | `xiyouji-agent-web`（CodeBuddy Agent SDK，`CODEBUDDY_API_KEY`） | 绕过 scripts/rag 直接对话 / 检索 docs / 跑 scripts |
| 部署 | GitHub Pages（`https://1273984347.github.io/xiyouji/`）+ PWA 离线 | 静态托管，无需服务器 |

---

## 四、目录结构划分

```
xiyouji/
├── docs/        十大板块 Markdown 解读（A1-A6 + 07/08/09 应用型 + 10 方法论）
│   ├── 00-导读/          项目说明 / 术语表 / 文档规范
│   ├── 01-全书逐回解读/  100 回逐回解读（A1，已收束）
│   ├── 02-人物深度分析/  取经五众 / 神佛 / 妖怪谱系（A3）
│   ├── 03-主题与情节专题/大闹天宫 / 八十一难 / 跨学科（A4）
│   ├── 04-文化与历史背景/成书 / 佛道 / 明代镜像（A5）
│   ├── 05-诗词歌赋/      原著诗词 + 个人创作（A6）
│   ├── 06-个人随笔/      现代视角解读（A2）
│   ├── 07-学以致用/ 08-提升认知/ 09-精神塑造/ 10-方法论沉淀(S1)/
│   ├── S2-外部分享/ S3-方法论外部分享/ S4-学术投稿/
├── source/      原著（分回/100回 + shendu/100篇深读切片）+ 引用（论文索引50 / 网络15）
├── site/        静态站：index/dashboard/mobile-index + chapters/characters/themes/
│                 data/(86 可视化页) + static/(css/js/fonts/images) + en/(英文站 ~65-70 文件)
├── scripts/     Python 34类分析(A-AH) + 截图审查 + 校验 + api/ + rag/ + output/(索引/产物)
├── timeline/    取经路线 / 大事年表 / 人物时间线
├── assets/ references/ tools/   图片字体占位 / 参考文献 / 章节切分
├── xiyouji-agent-web/  Web Agent「西游记·渡口问津」
├── mcp-server/  5 个只读 MCP 工具封装
└── 根文档        README / STRUCTURE / CHANGELOG / 交接文档 / DESIGN / CONTRIBUTING …
```

**内容规模（锚定 verify_delivery.py，含文档滞后说明）**：
- A1 逐回 100/100（含索引 101）
- A3 人物 ≈199–212 篇（脚本硬门禁值偏低端，doc 声明偏高）
- A4 主题专题 = **201 篇（verify 硬门禁）**，doc 声明常写 209–210
- A5 文化 34 篇 · A6 诗词 ≈13–14 篇 · A2 随笔 ≈44–45 篇
- docs 合计约 645 篇；A1–A6 内容向约 617 篇
- site/data 可视化页 **86 个**；dataset JSON **约 41–42 个**；英文站 `site/en/` 约 65–70 文件

---

## 五、核心模块职责

- **静态站点**：`index.html`（九卷索引档案表 + 问渡口 Hero）、`dashboard.html`（50+ KPI + 全站搜索浮层）、`mobile-index.html`（375px PWA 入口）、`guide.html`（7 类读者阅读路径）。
- **可视化页（86）**：A–AH 34 类 + Q+/Q++/Q+++ 批评史/弹幕 + 全文检索 + 图谱探索器（graph-explorer.html 纯 SVG 力导向）。
- **数据 API** `scripts/api/api_server.py`：零依赖，`/datasets` `/dataset/<name>` `/search` `/graph` `/graph/<name>` 等，约 41 数据集。
- **本地 RAG** `scripts/rag/`：LightRAG 架构零依赖落地（BM25+图谱）；`dukou-engine.html` + `rag-chat.js` 前端对话，离线回退模板引擎；W402 起 `LLM_API_KEY` 接通真实生成。
- **知识图谱**：`graph-explorer.html` 多图切换（佛学=AI=西游三元映射 / 取经人物关系）+ 边关系语义着色 / 钻取。
- **工程脚本**：`batch_screenshots.js`/`slice_screenshots.py`/`detect_unwrapped_tables.py`（截图审查）、`verify_delivery.py`（六文档同步分级校验）、`bump_version.py`（里程碑一键补齐）、`pre-commit-validate.py`（计数门禁）。
- **MCP 工具**：`mcp-server/` 5 个只读工具（drl_spotcheck / data_validate / docs_index / lint_links / a11y_audit）。
- **Web Agent**：`xiyouji-agent-web/`（CodeBuddy Agent SDK，`PROJECT_CWD=D:/1/xiyouji`），平行"能问"入口。

---

## 六、当前开发进度

**HEAD = v2.3.33 · W418（2026-08-10，内容质量深化）。**

里程碑（近期 W 段）：
- **W418** 内容质量深化：全站死链巡检（site/en/ 29 broken 链接修复——EN 版存在指向同目录/无 EN 版回退中文原版 ../data/*.html 加 lang="zh-CN" 标注·docs/ 全量 4784 链接 0 broken）+ A1 逐回解读 23 回补 `> 导航：` 引用行（100 回导航全覆盖 100/100）。
- **W390** GitHub Pages 上线 + rum.js 埋点（零服务器 localStorage 环形队列备援）。
- **W394–W398** 英文站 batch1–5 扩至约 65–70 文件（导览双页 + 12 张核心 data 可视化 + 地理簇）。
- **W399** CI 触发修复（补 push main）+ SEO 域名/sitemap + rum-viewer。
- **W400–W401** CI/安全 workflow 转绿（ruff 424 清零 / XSS high→0 / agent-web 构建入 CI，7 job 全绿）。
- **W402** **唯一长期阻塞解除**：档B 真实 LLM 生成接通（provider 化 + 本地 `.env LLM_API_KEY`）。
- **W403** 访问数据接入（localStorage 自建基线，159 页幂等注入）+ GoatCounter 升级路径保留。
- **W404–W405** S2 分发：44 篇随笔全发布（公众号/知乎风格）+ `curated.html` 精选合集 + 访问统计方案文档（Umami vs GoatCounter 六维对比 + 7 步升级）。
- **W406** 截图审查纳入发布流程（screenshot-review.yml 补 push main 触发 + batch_screenshots.js 良性过滤 file:// 回退噪声）。
- **W407** 修数据路径代码异味（P2）：dialogue-sentiment 补 `../../` + 两 -view 页 file:// 跳过 `/dataset/` 死 fetch。
- **W408** 修 static 资源路径（P2 续）：86 页 516 处 `static/` → `../static/`（静态 404 归零）。
- **W409** 文档同步刷新：交接文档内容纠偏 + 五文档版本叙述校准（v2.3.24/W409）。
- **W410** npm 依赖审计补充：agent-web 纳入 CI audit（security.yml npm-audit 双目录）+ 依赖链 overrides 修复（cherry-markdown 0.11.9/mermaid 11.16.1/dompurify 3.4.13·uuid 9→11·lucide-react 0.563→1.31·双目录 audit 0 漏洞）。
- **W411** 安全审计 P0-1/P1-1 处置：Web Agent 鉴权加固（可选 AGENT_WEB_TOKEN 认证 + 权限白名单禁外部 bypass + cwd 越界回落 PROJECT_CWD + 仅回环监听 127.0.0.1）+ MCP 4 工具路径白名单（is_relative_to 拒越界）。
- **W412** 安全审计剩余项处置：P0-2 禁运行时覆盖 CODEBUDDY_API_KEY/BASE_URL（前端表单移除·仅服务端 .env）·P1-2 静态站 XSS 转义 6 页·P2 RAG 参数钳制/LLM 端点校验/SSE 超时与断开清理/DOMPurify 消毒/依赖锁定·P3 日志收敛/CORS 白名单/死代码/版本漂移（P1-3 密钥吊销轮换需用户 DeepSeek 控制台操作）。
- **W413** 仓库文件策略审查：严格审查入库边界（6 个个人文档 + _dev/_templates/superpowers + 方法论沉淀均无敏感内容·全部恢复入库·仅硬性排除 .env 密钥/审计档/依赖/构建产物/可重建产物·字体源入库 +24MB·verify_delivery 恢复交接文档核心硬门禁）。
- **W414** README 用户手册改造：普通读者专区 + 开发者/维护者专区两级引导结构（顶部 GitHub Pages 在线站点一键直达 https://1273984347.github.io/xiyouji/·内容导航表·数据维度全景·目标读者清单；工程维护内容收进 `<details>` 折叠）。
- **W415** README 视觉引导增量：顶部徽章区 3 枚 shields.io（在线访问/双协议授权/部署状态）·「🎁 你将会看到什么」图标化速览 8 条·首页预览截图（assets/images/index-preview.png）·💬 反馈与建议段·技术栈段。
- **W416** 文件管控清单标注：文档规范.md 新增 §11（11.1 必同步 8 类附门禁 / 11.2 禁擅自修改 12 类附例外 / 11.3 接手速查 6 步）·交接文档接续流程新增文件管控步骤——多 session/Agent 协作文件权限显式化。
- **W417** 文档健康治理：CHANGELOG/file-index/交接文档归档精简降达标（<50KB/<500 行）·verify_delivery 新增 A1-A6 真实文件计数校验（611==611）·actions 升级消除 Node 20 deprecation（48 处）·RAG 索引重建演练通过·bump_version 增强（--desc/W 范围锚点/页脚同步）·LICENSE 双协议边界补强·sitemap 补全 154 页。
- **工程化 E1–E8 全完成**：CI/CD 矩阵、a11y 40 条 WCAG 2.2、perf_optimize、i18n 英文站、Playwright E2E 三层、Core Web Vitals RUM、SEO、安全加固（CSP/SRI/5 安全头）。
- **设计系统 W334**：全站「新中式·数字雅集」重设计（tokens 集中 + 子集化字体 + 88 页换肤）。

---

## 七、待办事项清单（接手可推进）

**已勾选（近期完成）**：截图审查纳入发布流程（W406）、发布与可发现性（W390/W399/W394-W398）、档B 真实生成（W402）、访问数据接入（W403）、S2 全分发（W404-W405）。

**仍开放的真实待办**：
1. **[ ] 真实跨访客读者量验证** — 注册 GoatCounter（或 Netlify 部署使 CSP 生效），跑 `inject_goatcounter.py --site xxx` 切换真实统计；有流量则继续分发（S4 学术投稿），无则停内容生产。
2. **[ ] 英文站续译** — `site/data` 剩余约 80+ 张可视化页英文化（重页如 character-dynamic-network 因脚本内嵌 ~55 条中英混排串易碎，须先枚举字面量）；可复用 `scripts/_extract_strings.py` + `_validate_en.py`。
3. **[ ] 内容质量深化（非增量）** — A1 逐回交叉引用/结构化元数据、A5/A6 提质、全站死链与术语统一。
4. **[ ] RAG/可视化增强** — 可选 embedding 向量检索；V2 新增维度（3D 地理 / 动态关系网络）。

**已收敛/冻结（勿再投入）**：A3、A4 模板化扩容（仅允非模板 gap-fill）；S3 方法论外部分享（13 篇全发布）。

---

## 八、接手必读雷区与铁律（摘要）

| # | 铁律/雷区 | 处置 |
|---|---|---|
| 1 | D3 `file://` 限制 | 禁 fetch 跨域 JSON；必须 `EMBEDDED_DATA fallback`；改数据同步两处（内嵌 + `scripts/output/data/*.json`） |
| 2 | **双索引** | 每文件 + 每 W### 必同时登记 CHANGELOG 与 file-index，不可只改其一 |
| 3 | **六文档同步（W393 降级）** | 核心 2 硬门禁（CHANGELOG/交接文档，缺失 v/W 阻断 commit）+ 辅助 4 WARN（README/STRUCTURE/项目说明/file-index） |
| 4 | **E1 跨 session git tracked** | 每 W 落地后 `git ls-files` 验证是否真 tracked，防 memory 层假收敛 |
| 5 | **Preflight 三轨验证** | line 号归属 + 内容匹配 + chapter 归属，创作前必验（`num:N` Grep） |
| 6 | subagent 证据不可盲信 | 主代理必须对关键声明直接 Read/Grep spot-check |
| 7 | CSS 27 token | 集中 tokens.css，Hero 渐变固定值，禁裸色值；改配色全页同步 |
| 8 | 空数据/响应式 | 渲染函数空值守卫开头；768px / 520px 必过视觉验证；div 交互须 role+tabindex+键盘 |
| 9 | GitHub Workflow | 项目直 push main（非 PR），CI 仅 push 触发；新增 workflow 勿与 pages.yml 竞态 |

**战略提醒**：内容模板化扩容边际收益已趋零，重心在"能问能搜的活系统"（RAG 真实生成 / 数据 API / 图谱 / 发布 / 英文站），而非再加静态篇目。

---

*本文档为接手认知汇总，细节以各源文件为准。关键源文件：交接文档.md · CHANGELOG.md · STRUCTURE.md · DESIGN.md · README.md · docs/00-导读/项目说明.md · 项目交接参考手册.md · 项目认知总览.md*
