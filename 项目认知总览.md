# 详解西游记 · 项目认知总览

> 独立存档版，用于接手与开发前的整体认知建立。
> 提取时间：2026-08-12 · 实测 git HEAD = **v2.3.39 · W424**（已 commit，待 push）。
> 来源：交接文档.md / README.md / STRUCTURE.md / DESIGN.md / 项目交接参考手册.md / docs/00-导读/项目说明.md + git 实测。
> 说明：文档声明计数与目录实际值偶有 +1 漂移，本文以**目录实测为准**；交接文档「当前版本号」段已于 W414 六文档同步校准，与 HEAD 一致。

---

## 一、背景缘起

- **创建于 2026-07-21**，单人项目（GitHub 账户 `1273984347`），「真人 + AI 代理」协作产出。
- **核心理念「一源多形」**：把《西游记》解读工作组织为四层——原著文本 → Markdown 解读 → 可浏览 HTML 站点 → Python 数据分析。不替读者读完原著，而是把"重读痕迹、反复母题、被争论四百年的问题"安顿下来。
- **写作动机**：每一代人有自己的西游（明人讽喻、清人证道、20 世纪革命、21 世纪职场），项目想把"解读过程本身"整理出来，让下一个读者不必从零开始。
- **协作模式**：主代理派发 + subagent 并行产出 + 主代理 spot-check 复核，用 **W### 编号**串联「双索引」（CHANGELOG 正向时间线 + file-index 反向文件索引）。

## 二、业务目标

四条产品目标：**可读**（每篇独立成章）、**可查**（人物/回目/主题互链）、**可扩**（目录与命名稳定）、**可算**（原著可被脚本处理产出词频/关系图等）。

内容分层（docs/ 十大板块，目录实测）：

| 板块 | 内容 | 实际规模 |
|---|---|---|
| 00-导读 | 项目说明/术语表/文档规范 | — |
| 01-全书逐回解读 | 100 回逐回解读（A1） | 101 篇（100 回 + 索引）|
| 02-人物深度分析 | 取经五众/神佛/妖怪谱系（A3） | 212 篇 |
| 03-主题与情节专题 | 大闹天宫/八十一难/跨学科（A4） | 210 篇 |
| 04-文化与历史背景 | 成书/佛道/明代镜像（A5） | 35 篇 |
| 05-诗词歌赋 | 原著诗词 + 个人创作（A6） | 14 篇 |
| 06-个人随笔 | 现代视角解读（A2） | 45 篇 |
| 07-学以致用 | 应用型 | 5 篇 |
| 08-提升认知 | 应用型 | 4 篇 |
| 09-精神塑造 | 应用型 | 5 篇 |
| 10-方法论沉淀 | 项目复利经验库（S1） | 14 篇 |

> docs 合计 756 篇（除各板块 README）；A1-A6 内容向合计 611 篇（A1 100 + A2 44 + A3 211 + A4 209 + A5 34 + A6 13）。W422 已按目录实测校准，真实源以目录为准。

## 三、技术栈选型

| 层 | 选型 | 关键决策 |
|---|---|---|
| 前端 | 纯静态 HTML + **D3.js v7 CDN** + Three.js（3D 页） | **零依赖、file:// 可直接打开**；数据内嵌 `EMBEDDED_DATA` fallback，禁 fetch 跨域 JSON |
| 设计系统 | `tokens.css` + `system.css`（新中式·数字雅集，W334 重设计） | 27 个 CSS 变量集中 `:root`，禁裸色值；Hero 渐变固定；宋体/等宽字体栈 + 子集化 woff2 |
| 后端（可选） | Python **stdlib** http.server（零第三方依赖） | 数据 API（8787）、RAG 后端（8777）均为零依赖 |
| 文本分析 | Python（jieba/Pillow，按 A-AH 34 类组织 130+ 脚本） | 词频/共现网络/情感分析等 |
| RAG | 自研零依赖 BM25 + 图谱层，**LLM_API_KEY 存在时走真实生成** | W402 已接通（provider 化 Base URL，7 厂商 + 代理网关） |
| 测试/CI | Playwright E2E 三层 + pytest + GitHub Actions（4-5 workflow） | 全 CI 已转绿（CI/security/deploy） |
| Agent 平行入口 | `xiyouji-agent-web`（CodeBuddy Agent SDK，`CODEBUDDY_API_KEY`） | 绕过 scripts/rag 直接对话/检索 docs |
| 部署 | GitHub Pages（`https://1273984347.github.io/xiyouji/`）+ PWA 离线 | 静态托管，无需服务器 |

## 四、目录结构划分

```
xiyouji/
├── docs/        十大板块 Markdown 解读（756 篇·除 README）
├── source/      原著（分回/100回 + shendu/100篇深读切片）+ 引用（论文索引50/网络15）
├── site/        静态站：index/dashboard/mobile-index + chapters/characters/themes/
│                 data/(86 可视化页) + static/(css/js/fonts) + en/(英文站65文件)
├── scripts/     Python 34类分析(A-AH) + 截图审查 + 校验 + api/ + rag/ + output/(索引/产物)
├── timeline/    取经路线/大事年表/人物时间线
├── assets/ references/ tools/   图片字体占位 / 参考文献 / 章节切分
├── xiyouji-agent-web/  Web Agent「西游记·渡口问津」
├── mcp-server/  5 个只读 MCP 工具封装
└── 根文档        README / STRUCTURE / CHANGELOG / 交接文档 / DESIGN / CONTRIBUTING …
```

## 五、核心模块职责

- **静态站点**：`index.html`（九卷索引档案表）、`dashboard.html`（50+ KPI + 全站搜索浮层）、`mobile-index.html`（375px PWA 入口）。
- **可视化页（86）**：A-AH 34 类 + Q+/Q++/Q+++ 批评史/弹幕 + 全文检索 + 图谱探索器（graph-explorer.html 纯 SVG 力导向）。
- **数据 API** `scripts/api/api_server.py`：零依赖，`/datasets` `/search` `/graph` 等，41 数据集。
- **本地 RAG** `scripts/rag/`：LightRAG 架构零依赖落地（BM25+图谱）；`dukou-engine.html` + `rag-chat.js` 前端对话，离线回退模板引擎。
- **知识图谱**：`graph-explorer.html` 多图切换（佛学=AI=西游三元映射 / 取经人物关系）。
- **工程脚本**：`batch_screenshots.js`/`slice_screenshots.py`/`detect_unwrapped_tables.py`（截图审查）、`pre-commit-validate.py`（计数门禁）、`verify_delivery.py`（六文档同步分级校验）、`bump_version.py`（里程碑一键补齐）。
- **MCP 工具**：`drl_spotcheck/data_validate/docs_index/lint_links/a11y_audit` 5 个只读工具。

## 六、当前开发进度

**HEAD = v2.3.39 · W424（2026-08-12，对抗性审查修正与全仓整理：A4 门禁校准 209·3D/EN 页修复·security 门禁修复·CI 文档同步·产物清理）。**

里程碑演进（实测 + 文档交叉确认）：
- **内容向已收束**：A1 逐回 100/100；A3=211、A4=209（W342 后冻结，仅允非模板 gap-fill）；A6 中英文专题无剩余 gap；S3 方法论外部分享 13 篇全部发布。
- **W390 起上线发布**：GitHub Pages 部署 + W399 SEO/sitemap 域名补全 + W394-W398 英文站 batch1-5 扩至 65 文件。
- **W402 解除唯一长期阻塞**：档B 真实 LLM 生成接通（provider 化 + 本地 .env `LLM_API_KEY`）。
- **W403 访问数据接入**：localStorage 自建基线（159 页幂等注入）+ GoatCounter 升级路径保留。
- **W404-W405 S2 分发**：44 篇随笔全发布（公众号/知乎风格发布版）+ `curated.html` 精选合集 + 访问统计方案文档（Umami vs GoatCounter 六维对比 + 7 步升级）。
- **工程化 E1-E8 全完成**：CI/CD 矩阵、a11y 40 条 WCAG 2.2 规则、性能优化、i18n 英文站、Playwright E2E 三层、Core Web Vitals RUM、SEO、安全加固（CSP/SRI/5 安全头）。
- **设计系统**：W334 全站「新中式·数字雅集」重设计（tokens 集中 + 子集化字体 + 88 页换肤）。

## 七、待办事项清单（接手可推进）

**仍开放的真实待办（来自交接文档/参考手册「待办」段 + 实测）：**
1. **增强版截图审查纳入迭代发布流程** — 内容/样式大幅改动后重跑 `npm run screenshots`（勾选但未完成）。
2. **真实跨访客读者量验证** — 需注册 GoatCounter 或部署到支持 `_headers` 的 Netlify（GitHub Pages 不应用 CSP），有流量则继续分发（S4 学术投稿），无则停内容生产。
3. **内容质量深化（非增量）** — A1 逐回交叉引用/结构化元数据、A5/A6 提质、全站死链与术语统一。
4. **英文站续译** — batch1-5 已完成 65 文件，剩余约 80+ 张 data 可视化页（重页如 character-dynamic-network 因脚本内嵌 ~55 条中英混排串易碎，需专项深译）。
5. **RAG/可视化增强** — 可选 embedding 向量检索；V2 新增维度（3D 地理 / 动态关系网络）。

**已收敛/冻结（勿再投入）：** A3(212)/A4(210) 模板化扩容、S3 方法论外部分享、W286 系列源文件整理。

## 八、接手必读雷区与铁律

| # | 铁律/雷区 | 处置 |
|---|---|---|
| 1 | D3 `file://` 限制 | 禁 fetch 跨域 JSON；必须 `EMBEDDED_DATA fallback`；改数据同步两处 |
| 2 | **双索引** | 每文件 + 每 W### 必同时登记 CHANGELOG 与 file-index，不可只改其一 |
| 3 | **六文档同步** | 核心 2 硬门禁（CHANGELOG/交接文档，缺失 v/W 阻断）+ 辅助 4 WARN（README/STRUCTURE/项目说明/file-index） |
| 4 | **E1 跨 session git tracked** | W 落地后 `git ls-files` 验证是否真 tracked，防 memory 层假收敛 |
| 5 | **Preflight 三轨验证** | line 号归属 + 内容匹配 + chapter 归属 Grep `num:N`，创作前必验 |
| 6 | subagent 证据不可盲信 | 主代理必须对关键声明直接 Read/Grep spot-check |
| 7 | CSS 27 token | 集中 tokens.css，Hero 渐变固定值，禁裸色值；改配色全页同步 |
| 8 | 空数据/响应式 | 渲染函数空值守卫开头；768px/520px 必过视觉验证；div 交互须 role+tabindex+键盘 |

**战略提醒**：内容模板化扩容边际收益已趋零，**重心在"能问能搜的活系统"（RAG 真实生成/数据 API/图谱/发布/英文站），而非再加静态篇目**。

---

*本文档为信息汇总，细节以各源文件为准。关键源文件：交接文档.md · CHANGELOG.md · STRUCTURE.md · DESIGN.md · README.md · docs/00-导读/项目说明.md · 项目交接参考手册.md*
