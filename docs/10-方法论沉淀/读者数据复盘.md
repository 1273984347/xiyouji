# 读者数据复盘

> 用途：Phase 3 路线图 W465 决策闸门（W530 重排落地）的判定记录与数据存档。GoatCounter 后台为唯一真实读者源（`visit-log.js` 仅本地诊断、明确不计入）。
> 创建：2026-08-26（W530）· 判定执行：待数据回填 · **W535：UV 取数已自动化**（`scripts/fetch_gate_stats.py`，见第零节）

## 第零、UV 取数方式（W535 自动化 · 替代人工抄表）

> 前两项（7 日 / 30 日 UV）不再需要登后台手抄，一条命令即可取回并可随时复算。

**一次性准备**（只需做一次）：GoatCounter 后台 → 右上角用户名 → API → 生成密钥 → 写入仓库根 `.env`：

```
GOATCOUNTER_API_TOKEN=你的密钥
```

`.env` 已在 .gitignore 内，**严禁入库**。

**取数 + 判定**：

```bash
py -3 scripts/fetch_gate_stats.py                 # 输出 uv7 / uv30 + 推荐命令
py -3 scripts/judge_gate.py --uv7 <值> --uv30 <值> --report
```

口径与实现要点（接口依据为 2026-08-30 核对的官方 OpenAPI `https://www.goatcounter.com/api.json`）：

- 端点 `GET /api/v0/stats/total?start=YYYY-MM-DD&end=YYYY-MM-DD`，鉴权 `Authorization: Bearer <token>`
- **页面访客 UV = `total` − `total_events`**（GoatCounter 的 `total` 含事件访客，闸门口径要的是页面访客）
- 时间窗为闭区间且含今天：近 7 日 = `[today-6, today]`，近 30 日 = `[today-29, today]`
- 脚本自带 `--self-test`（14 项，覆盖缺字段 / 事件数倒挂 / 401·403 / 缺令牌等负样本）与 `--fixture`（离线演练，不联网）
- 后三项（UV 曲线 / 来源占比 / 跳出率）API 不直接提供等价口径，仍需后台截图留档

## 一、首轮数据（5 项数字）

| 指标 | 数值 | 来源 / 说明 |
|:---|:---|:---|
| 7 日 UV | ⬜ 待回填 | `py -3 scripts/fetch_gate_stats.py`（W535 起；此前口径为后台统计页手抄） |
| 30 日 UV | ⬜ 待回填 | 同上 |
| UV 曲线 | ⬜ 待回填 | 后台趋势图截图留档 |
| 来源占比 | ⬜ 待回填 | 后台 Referrer 分布 |
| 跳出率 | ⬜ 待回填 | 后台 Bounce 统计 |

## 二、污染确认（Step 1）

- R6 拦截已落地（W529）：截图 / LHCI / 冒烟均不再向 GoatCounter 发 beacon（实测 `goatcounter.js` 自带 localhost/file 排除 + 5 处纵深防御拦截）。
- 后台复核：push 后新增访客 UA 列表截图留档，确认无 `HeadlessChrome`（完成时间：____）。
- 判定输入可信度结论：⬜ 数据可信 / ⬜ 存在疑点（说明：____）

## 三、判定记录（Step 3 · judge_gate.py --report 自动追加）

> 由 `py -3 scripts/judge_gate.py --uv7 <值> --uv30 <值> --report` 追加，含输入值 + 阈值 + 输出分支 + 判定时间，他人可复算。

## 四、决策与后续动作

- 输出分支：⬜ 分发（启动 Phase B）/ ⬜ 归档（停止内容生产·转纯维护）/ ⬜ 中间态（继续观测 + Phase C 加固）
- 后续批次安排：________
