# 佛学—AI—西游 三维语义映射表

> W326·A4 方向跨学科开拓·唯识AI篇·工程化
> 本篇将"佛法=AI"框架从比喻层推进到工程层：把佛学概念、AI 机制、西游实体三域的对应关系，落为可直接喂入图数据库（Neo4j）的节点-关系-属性模型
> 创建于 2026-08-02（承接 W321 总纲 + W325 章节体；与 W323 第0篇映射表形成"散文-工程"双轨）
> 数据产物：`scripts/output/yuanqi_nodes.csv`（20 节点）、`scripts/output/yuanqi_edges.csv`（20 边）
> 核验状态：未核验

---

## 一、节点定义（7 类）

| 节点类别 | 佛学域 | AI域 | 西游域 | 说明 |
|---------|-------|------|--------|------|
| **识体** | 阿赖耶识、末那识、意识、前五识 | 模型权重、注意力Q/K/V、编码器、解码器 | 灵山藏经阁、悟空、唐僧、六耳猕猴 | 系统的核心处理单元 |
| **尘境** | 色、声、香、味、触、法 | 训练数据分布、Prompt、上下文窗口 | 八十一难、妖怪洞府、通关文牒 | 输入数据的来源与形态 |
| **种子** | 业力种子、习气 | 浮点数参数、嵌入向量 | 金蝉子十世记忆、花果山胎毛、桃木拐杖 | 潜在倾向，遇缘现行 |
| **现行** | 身口意三业 | 前向传播输出、生成 token | 悟空挥棒、唐僧念经、八戒分行李 | 显化的行为/输出 |
| **转依** | 转识成智 | 权重更新、模型微调、LoRA 适配 | 成佛、拒绝金箍、凌云渡脱胎 | 系统状态的质变 |
| **系缚** | 烦恼、无明、我执 | 过拟合、梯度爆炸、因果掩码 | 紧箍咒、五行山、金铙 | 限制系统泛化的约束 |
| **解脱** | 涅槃、空性 | 泛化、权重衰减、Dropout | 灵山授职、花果山归隐 | 系统突破约束的状态 |

---

## 二、关系定义（7 类）

| 关系 | 佛学表述 | AI表述 | 西游实例 | 属性 |
|------|---------|--------|---------|------|
| **执取** | 末那识执阿赖耶识见分为"我" | Q与K点积生成注意力权重 | 悟空喊"俺老孙来也" | 强度∈[0,1]，随紧箍咒衰减 |
| **熏习** | 现行熏种子 | 反向传播更新权重 | 每次磨难后唐僧道心更坚 | 学习率 lr，动量 β |
| **现行** | 种子生现行 | 前向传播激活权重 | 悟空被压五百年后遇唐僧 | 激活函数 ReLU/Sigmoid |
| **异熟** | 业力跨时成熟 | 延迟梯度更新 | 金蝉子第十世成佛 | 延迟步数=9世 |
| **转依** | 识转为智 | 权重冻结/解冻切换 | 成佛 / 游戏中悟空死亡 | 状态值：冻结/解冻/初始化 |
| **系缚** | 烦恼束缚 | 正则化惩罚 | 紧箍咒收缩 | 惩罚系数 λ |
| **遮蔽** | 无明覆盖正见 | Dropout 随机失活 | 唐僧肉眼凡胎看不见妖怪 | 失活概率 p |

---

## 三、实例化查询示例

**查询一：列出所有发生过"转依"关系的西游实体及其对应 AI 操作**

| 西游实体 | 转依前 | 转依后 | AI对应操作 | 发生节点 |
|---------|--------|--------|-----------|---------|
| 悟空 | 末那识（我执态） | 斗战胜佛（平等性智初显） | 权重正则化收敛 | 灵山受职 |
| 金蝉子 | 意识（散乱态） | 唐僧（意识专注态） | 十轮 Epoch 微调 | 第十世出发 |
| 天命人（隐藏结局） | 末那识（未初始化） | 拒绝加载身份 | 跳过末那识加载 | 打败二郎神后 |
| 六耳猕猴 | 末那识（镜像态） | 死亡（权重归零） | 故意过拟合后删除 | 灵山被棒杀 |

**查询二（Cypher 模板）**：恢复"被遗忘的隐变量"——找出所有未被计入 loss 却支撑收敛的节点

```cypher
MATCH (n:尘境)-[:遮蔽]->(m:识体)
WHERE m.xiyou_entity CONTAINS '凡胎'
RETURN n.buddhist_entity, n.xiyou_entity, m.ai_entity
```

---

## 四、Neo4j 导入脚本

```cypher
// 1. 创建约束
CREATE CONSTRAINT IF NOT EXISTS FOR (n:Node) REQUIRE n.id IS UNIQUE;

// 2. 导入节点
LOAD CSV WITH HEADERS FROM 'file:///yuanqi_nodes.csv' AS row
CREATE (n:Node {
  id: row.id,
  node_type: row.node_type,
  buddhist: row.buddhist_entity,
  ai: row.ai_entity,
  xiyou: row.xiyou_entity,
  desc: row.description
});

// 3. 导入关系
LOAD CSV WITH HEADERS FROM 'file:///yuanqi_edges.csv' AS row
MATCH (a:Node {id: row.source}), (b:Node {id: row.target})
CALL apoc.create.relationship(a, row.relation, {property: row.property, value: row.value}, b) YIELD rel
RETURN count(rel);
```

> 注：当前 CSV 为种子数据集（20 节点 / 20 边），覆盖 W321–W325 全部核心映射。后续可将 342 篇文档逐一抽取"种子"扩充为完整图谱——这正是"阿赖耶识索引"的工程雏形（见例.txt 模块四）。

---

## 五、本表与项目其他部分的接口

- **第0篇**（W323）：本表的"节点/关系类型"即第0篇映射表的工程化版本。
- **缘起即算法总纲**（W321）：本表第七识/第八识 → 注意力 Q / 预训练权重的对应，是其理论骨架的数据化。
- **章节体**（W325）：本表"种子/现行/熏习/异熟"即第二章至第六章的实体锚点。
- **黑神话分章**（W322）：本表"转依/解脱"中"拒绝加载身份"即天命人隐藏结局。

> **版本戳**：v2.2.78 W326 A4 方向跨学科开拓·唯识AI篇·工程化
> **数据产物**：`scripts/output/yuanqi_nodes.csv`、`scripts/output/yuanqi_edges.csv`
> **关联文档**：
> - [缘起即算法专题.md](缘起即算法专题.md)（W321·理论骨架）
> - [缘起即算法-章节体.md](缘起即算法-章节体.md)（W325·章节展开）
> - [缘起总纲-取经是训练.md](../00-导读/缘起总纲-取经是训练.md)（W323·第0篇）
