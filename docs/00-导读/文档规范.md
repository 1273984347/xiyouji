# 文档规范

> 本文件是项目文档的**元规则**（rules for rules）：定义每份文档「写什么、怎么写、写到多大、何时归档、如何机器校验」。
> 适用对象：所有更新本项目文档的人与 AI 代理（含 Web Agent「写作助手」角色）。
> 创建于 2026-08-03（W336 文档瘦身复盘）；2026-08-09 按业界标准（Keep a Changelog / SemVer / Conventional Commits / Diátaxis / awesome-readme）重构。
> 配套门禁：`scripts/verify_delivery.py`（pre-commit 强制）·归档区：CHANGELOG-ARCHIVE.md / file-index-archive.md / 交接文档-archive.md。

## 1. 设计目标

| 目标 | 含义 | 对应机制 |
|------|------|---------|
| 单一事实源 | 每个事实只有一个权威文档，其余只放指针 | §3 角色表（禁止列） |
| 防膨胀 | 每份文档有写入上限与归档触发，控 token 成本与 diff 噪音 | §5/§8 |
| 可机器校验 | 核心约束由 pre-commit 门禁强制，不依赖人工自觉 | §8 |
| 可跨 session 接续 | 新 session 5 分钟内能定位当前进度与下一动作 | 交接文档.md |
| 可追溯 | 每个变更可回溯到 W### 与 commit | CHANGELOG + file-index + §6 |

## 2. 对齐的业界标准

| 标准 | 适用范围 | 本项目落地 |
|------|---------|-----------|
| [Keep a Changelog](https://keepachangelog.com) | CHANGELOG.md | 六类变更标记 + Unreleased 段（§5） |
| [SemVer](https://semver.org) | 版本号 | vMAJOR.MINOR.PATCH：破坏性变更升 MAJOR，新功能升 MINOR，修复升 PATCH |
| [Conventional Commits](https://www.conventionalcommits.org) | git commit message | `<type>(<scope>): <subject>`（§6） |
| [Diátaxis](https://diataxis.fr) | 文档组织 | 四象限定位（§3 角色列） |
| awesome-readme 精神 | README.md | 项目是什么 → 怎么用 → 贡献（§3） |

Diátaxis 四象限速查：**Tutorial**=教学上手（学概念）·**How-to**=操作指南（解决问题）·**Reference**=权威参考（查事实）·**Explanation**=背景解释（理解为什么）。

## 3. 文档角色与单一事实源

| 文件 | Diátaxis 角色 | 职责 | 每个 W### 写入量上限 | 禁止写入 |
|------|--------------|------|---------------------|----------|
| CHANGELOG.md | Reference | 施工日志（唯一允许写细节的地方） | 10-20 行（来源/文件/验证/状态四件套） | 无限制，但超过 30 行需自问是否必要 |
| README.md | Explanation + Reference | 项目是什么 + 怎么用 | 0 行（仅更新版本号） | 禁止写 W### 细节 |
| STRUCTURE.md | Reference | 目录结构说明 | 0 行（仅版本号 + 新增目录） | 禁止写 W### 细节 |
| 项目说明.md | Reference | 项目状态快照 | 0 行（仅版本号） | 禁止写 W### 细节 |
| file-index.md | Reference | 文件→W 反向索引 | 每文件 1 行（文件名 + W### + 一句话） | 禁止写验证过程/subagent 报告 |
| 交接文档.md | Explanation | 跨 session 交接 | 每个里程碑 3-5 行摘要 | 禁止复制 CHANGELOG 完整条目 |
| 术语表.md | Reference | 术语唯一释义 | 按需 | 禁止解释性论述（指回引用处） |

判据：想写一段内容时，先问「哪个文件是它的唯一事实源？」，不落在该文件 → 只放指针或交给 CHANGELOG。

## 4. 写作规则

### 4.1 Markdown 基础

- 标题层级 ≤ 3 级（`#` 文件标题 / `##` 章节 / `###` 小节），避免深层嵌套
- 代码块必须标注语言（` ```python ` / ` ```json `），便于高亮与 lint
- 仓库内链接用相对路径；外部链接用绝对 URL；链接文本要能自解释（禁止「点这里」「此处」）
- 行宽 ≤ 120 字符；版本描述行 ≤ 200 字符（门禁实测值）
- 引用 W 用三位数 `W###`（如 `W413`）；版本用 `vX.Y.Z`（如 `v2.3.28`）

### 4.2 版本描述规则（README / STRUCTURE / 项目说明）

- **格式**：`> 当前版本 vX.X.XX（日期）：一句话概括项目规模 + 最近 1-2 个里程碑关键词。详细变更见 CHANGELOG.md。`
- **长度**：≤ 200 字符
- **禁止**：在版本行中列举所有历史 W### 的内容（历史见 CHANGELOG）
- **更新**：由 `scripts/bump_version.py` 从 `site/dukou-engine.html` 页脚读取 v/W 一键同步

### 4.3 术语一致性

- 首次使用术语时链接到 [术语表.md](术语表.md)；同一概念全文用同一词（如「渡口问津」「心猿」不混写别名）
- 避免同义词漂移：一处用了「待办」，别处不要再引入「TODO/下一步事项」指同一概念

### 4.4 命名规范

- 文档文件名：语义名（中文或 kebab-case），**不**含版本号（版本写在内容头部）
- 引用版本段格式：`### vX.Y.Z（日期）：W### 一句话主题`
- 归档文件名：`<原名>-archive.md`（与现役文件同目录，头部标注归档范围）

### 4.5 轨标体系与准入

- 轨标是文档开头第一条 `> 轨标：X` 行（现役轨标以此为准）；文末历史元数据块中的旧轨标是版本追溯记录，**不得修改**。
- 轨别定义：
  - **学术研究**：以可核查引用（论文 / 专著 / 版本 / 理论框架出处）支撑的严肃分析（引用池见 `source/引用与网络解读/学术论文索引.md`）；仅凭原著文本证据（关联回目 / 数据指标）不构成引用
  - **跨界趣谈**：把现代学科或生活概念作为趣味透镜套用到《西游记》的联想型文章（如 西游与经济学、八十一难风险管理学），不声称学术贡献
  - **教学讲解**：面向普通读者的普及讲解（逐回解读、人物介绍）
  - **个人创作**：文学性创作（外传、深读随笔、方向二深化）
  - **大众普及**：面向大众的普及文章（当前暂无存量，保留轨位）
  - **外部分享**：S3 方法论外部分享
- 准入规则：
  1. 标「学术研究」必须至少有一条可核查引用（论文 / 专著 / 版本 / 理论框架出处），否则应降为「跨界趣谈」或「教学讲解」
  2. 「跨界趣谈」不得以「学术研究」身份对外宣传，也不得出现在「学术研究者」目标读者说明中
  3. 新增文档必须先定轨别再写作；轨别修改走常规 W### 变更流程
  4. 「学术研究」文档头部必须含 `> 引用：` 行，显式链接 `source/引用与网络解读/学术论文索引.md`（W452 起由 verify_delivery 机器校验）
- 历史：W450 将 33 个「现代学科趣味透镜」文档从 学术研究 批量改标为 跨界趣谈（清单见 CHANGELOG W450）；W451 引用审计将 9 个无引用文档从 学术研究 降为 教学讲解（清单见 CHANGELOG W451）；W452 为全部 105 篇现役「学术研究」补齐 `> 引用：` 显式链接并纳入机器门禁

### 4.6 元信息块 v2（血缘 + 核验状态 · W501）

- 适用范围：W501 起 docs/01–06 的**新增**文件；存量 611 篇由基线清单 `scripts/output/frontmatter-baseline.txt` 豁免（核验状态字段由 W504 统一补标）。
- 新文件在轨标块（或「W 溯源」块）之后**必须**追加以下 4 行 blockquote：

| 字段 | 格式 | 取值规则 |
|------|------|---------|
| 生成来源 | `> 生成来源：X` | skill 生成填 `<skill短名>@<commit短哈希>`（如 `xiyouji-character-content@aa410f9`）；人工撰写填 `人工撰写` |
| 生成模型 | `> 生成模型：X` | 执行 agent 能可靠自报的模型名；无法自报**必须**填 `未记录`（禁止编造模型名）；人工撰写填 `不适用` |
| 生成日期 | `> 生成日期：YYYY-MM-DD` | 内容产出日期 |
| 核验状态 | `> 核验状态：X` | 三值枚举：`未核验`（初始值）/ `引文已核验`（引文行 ≥ 3 且对 `dataset/text-search.json` 命中率 100%，由 `scripts/check_citations.py` 验证）/ `专家已核验`（人类专家确认）。**空真防护：0 条引文的文档禁止标 `引文已核验`** |

- 机器门禁：`scripts/check_frontmatter.py`（verify_delivery 第 18 门禁，W501 挂载），只扫描不在基线清单内的新文件；缺字段、枚举非法、日期格式错误 = FAIL。
- 设计依据：2026-08-24《内容可信度与溯源体系》方案（docs/superpowers/plans/2026-08-24-content-trust-provenance-w501-w505.md）W501。

### 4.7 术语一致性（机器可读库 + 锚定门禁 · W502）

- 单一事实源：`docs/00-导读/术语表.md`（人类可读）→ `scripts/check_glossary.py --generate` 逐行解析生成 `dataset/glossary.json`（机器可读，禁止手改）。改术语表后必须重跑 `--generate`。
- C1 双向同步：术语表.md 与 glossary.json 逐组逐条核对，diff = 0（防两侧漂移）。
- C2 规范词锚定（仅「一、人物称谓」组）：文档含某变体称谓（心猿/行者/金公/木母/黄婆/意马/唐僧/唐三藏/圣僧/弼马温/齐天大圣）但其规范词（指代，含传递归一：圣僧→唐僧→玄奘）出现 0 次 = 违规。复合词掩码：心猿意马/金公木母黄婆 先遮盖再扫描，避免子串误报。
- 基线豁免：存量违规 303 篇 383 条冻结于 `scripts/output/glossary-baseline.txt`（2026-08-24 实测），门禁只拦**新增**违规。
- 已知局限：CJK 子串匹配无词边界；新文档被误报时应在正文补规范词（而非改门禁）。
- 机器门禁：`scripts/check_glossary.py`（verify_delivery 第 19 门禁，W502 挂载）。

### 4.8 原著引文语法（精确命中 · W503）

- 引文行格式（单行，blockquote）：`> 原文引文（第N回）：“……”`——N ∈ 1–100，全角括号与全角引号。
- 命中规则：引文必须是 `dataset/text-search.json` 中 `chapters[N-1].text` 的**精确子串**；比较前双方去除全部空白（`re.sub(r'\s+','')`，兼容原文换行），除此之外逐字精确，不做任何标点/异体字宽容。
- 禁止省略号节引：需节引时拆成多条引文行，每条独立命中。
- 使用要求：方向二深化 / 深化专题 / 学术轨新文档须含 ≥ 3 条引文行（见 xiyouji-character-content skill）；`核验状态：引文已核验` 的前置条件即引文行 ≥ 3 且命中率 100%。
- 机器门禁：`scripts/check_citations.py`（verify_delivery 第 20 门禁，W503 挂载，`--dir docs` 全量）；任何文档任一引文行未命中 = FAIL（存量锚定引文行实测为 0，无历史豁免）。

## 5. CHANGELOG 写法（Keep a Changelog 化）

- **头部**：`## [Unreleased]` 段 + W### 编号规则说明行
- **版本段**：`### vX.Y.Z（日期）：W### 一句话主题`，段内四件套（来源/执行/验证/状态）逐条 `> - **键**：值`
- **六类变更标记**（借鉴 Keep a Changelog，按需标注在四件套内）：

| 标记 | 含义 | 示例 |
|------|------|------|
| Added | 新增 | 新增 SEC-005 扫描规则 |
| Changed | 修改行为 | save-env-config 改为禁运行时覆盖 |
| Fixed | 修复缺陷 | 修复 TS2802 Map 迭代 |
| Security | 安全修复 | 密钥轮换、XSS 转义 |
| Removed | 移除 | 移除 exec 死代码 |
| Deprecated | 弃用 | 旧端点标记弃用 |

- **处置收尾**：回退/删除/清理/审查类操作（如 workflow 删除、报告归档）同样必须记入对应 W 段的「处置收尾」字段——不因「非新功能」而跳过
- **行数**：10-20 行/段；> 30 行需自问是否可拆分或归档
- **归档**：> 50KB 或 500 行 → 迁移最早版本段至 CHANGELOG-ARCHIVE.md
- **二级归档（W513）**：CHANGELOG-ARCHIVE.md / file-index-archive.md / 交接文档-archive.md 任一超 **1MB** → 触发二级归档：最老块迁移至 `docs/archive/<原名>-tier2.md`（如 CHANGELOG-ARCHIVE-tier2.md），archive 保留较新段 + 头部指针；tier2 文件须登记进 verify_delivery `ARCHIVE_DOCS`（W513 已入例）；tier2 历史段同受 §11.2 禁改约束

## 6. Commit Message 规范（Conventional Commits）

格式：`<type>(<scope>): <subject>`

本项目 type 集：

| type | 含义 | 示例 |
|------|------|------|
| fix | 修复 | `fix(w413): 仓库文件策略审查 — …` |
| feat | 新功能 | `feat(rag): provider 化 Base URL` |
| docs | 文档 | `docs(w413): 六文档同步 v2.3.28` |
| chore | 杂务/门禁 | `chore(ci): 移除过期 cache` |
| refactor | 重构（无行为变化） | `refactor(site): 抽取 escapeHtml 工具函数` |
| perf | 性能 | `perf(rag): 索引缓存` |
| security | 安全 | `security(mcp): 路径白名单` |

规则：
- `scope`：子系统名（rag/mcp/site/api/ci）或 W###（`w413`），可省略
- `subject`：祈使语气、≤ 60 字符；英文首字母小写
- 正文（可选）：`- ` 条目式列要点；破坏性变更用 `BREAKING CHANGE:` 前缀
- 提交体关联 W###（如 W413），便于 git log 与 CHANGELOG 双向追溯

## 7. 六文档同步机制（W393 降级·保留）

旧做法（导致膨胀 + 工程期无效税）：每个 W### 完成后在 6 个文档各写一段完整描述。

降级后（内容已冻结，重心转工程化与发布）：

**核心 2 份（硬门禁，缺失 v/W 阻断 commit）** — 跨 session 连续性真载体：
1. **CHANGELOG.md**：完整四件套（来源/文件/验证/状态），10-20 行
2. **交接文档.md**：「已完成里程碑概要」顶部加 3-5 行摘要

**辅助 4 份（仅 WARN 不阻断）** — 每次 W### 的纯手工税，里程碑时一键补齐：
3. **README / STRUCTURE / 项目说明**：仅版本号字符串
4. **file-index.md**：每文件 1 行反向索引（W500 起结构健康由第 17 门禁 check_index_health.py 机器校验：段完整性/重复/最新段残留 FAIL 阻断·"仅 WARN"指版本行维度）

**同步辅助 4 份**：发布里程碑（或 `site/dukou-engine.html` 页脚版本前进）时跑 `python scripts/bump_version.py` 一键补齐到页脚当前 v/W。日常工程 W### 不必手改这 4 份——下次里程碑同步前仅 WARN，不阻断交付。

## 8. 自动化校验与健康度量

| 工具/门禁 | 校验内容 | 触发频率 |
|-----------|---------|---------|
| `scripts/verify_delivery.py` | 20 项门禁：六文档含页脚 v/W·范围漂移（html 最高 W ≤ 文档最高 W）·A4 计数（209 篇）·A1-A6 真实计数·A1 导航相邻性·docs/01 链接·sitemap 覆盖·数据漂移·CSP·腐蚀/插件·JS 语法·CSS 结构·token 覆盖率·动效禁止·a11y·INLINED CSS·动态链接·治理契约·skills 索引（W498）·索引健康（W500）·元信息块 v2（W501）·术语一致性（W502）·原著引文硬验证（W503） | pre-commit（每次 commit） |
| `scripts/security_scan.py` | 文档/代码敏感信息（SEC-005 sk- 密钥、CSP-001 头缺失） | CI Security |
| lint_links（MCP 工具） | 死链检测 | 里程碑 / 发布前 |
| bump_version.py | 辅助 4 份版本一致性 | 里程碑手动跑 |

健康指标（超标即行动）：
- CHANGELOG / file-index > 50KB 或 500 行 → 归档（§5）
- 归档三件套（CHANGELOG-ARCHIVE / file-index-archive / 交接文档-archive）任一 > 1MB → 二级归档（§5·W513）
- 交接文档「已完成里程碑概要」> 100 行 → 保留最近 5 个版本，其余删除
- 辅助文档 WARN 累积超过 1 个里程碑 → 跑 `bump_version.py`
- 敏感信息扫描 > 0 → 立即脱敏 + 密钥轮换

更新版本号时，在版本描述中保留对前一版本的引用（如「W334（v2.2.86）设计系统」），不删除所有旧版本痕迹。

## 9. 文档变更评审流程（docs-as-code）

1. **变更前**：查单一事实源是否已存在该信息（§3），存在则不重复
2. **变更中**：遵守 §4 写作规则与 §5/§6 格式；改历史段前按 E2 判据区分「历史事实（保留）」vs「现役描述（更新）」
3. **变更后**：跑 `verify_delivery.py`（提交门禁）确认 v/W 一致性与无范围漂移
4. 文档随代码走同一 commit/PR，禁止「只改文档不验证」；关键路径（版本/计数/W 范围）须 Grep spot-check 落地（E1 铁律）

## 10. 违反后果

文档膨胀/失配会导致：
- 新 session 加载上下文消耗大量 token（300KB 的 CHANGELOG ≈ 75K token）
- git diff 噪音淹没真正的代码变更
- 多 agent 协作时 Edit 冲突概率急剧上升（并行 Edit 竞态）
- 接手者阅读交接文档时信息过载，无法快速定位关键信息
- commit 被 pre-commit 门禁阻断（v/W 缺失或范围漂移），返工成本

## 11. 文件管控清单（多 session / 多 Agent 协作专用）

> 本节是 §3（角色表）+ §7（同步机制）的**显式化执行版**：本项目由多 session / 多 Agent 协作制作，
> 接手者必须先读本节——哪些文件每次 W### 完成后**必须同步**，哪些文件**禁止擅自修改**。
> 判据冲突时以本节为准；本节未列出的文件按 §3 角色表与 §7 处理。

### 11.1 必须同步的文件（每次 W### 完成后）

| 优先级 | 文件 | 必做动作 | 门禁 |
|--------|------|---------|------|
| 核心·硬门禁 | CHANGELOG.md | 新增 W### 版本段（来源/执行/验证/状态四件套）+ 编号规则 W001-W### | pre-commit `verify_delivery.py` 阻断 |
| 核心·硬门禁 | 交接文档.md | 阻塞段 HEAD 引用 + 当前进度段里程碑摘要 + 版本号列表 + 接续编号（当前 W###·下一 W###+1） | pre-commit `verify_delivery.py` 阻断 |
| 辅助·WARN | README.md | 版本号行 vX.Y.Z W###（≤200 字符） | `verify_delivery.py` WARN |
| 辅助·WARN | STRUCTURE.md | 版本号行 | `verify_delivery.py` WARN |
| 辅助·WARN | docs/00-导读/项目说明.md | 版本号行 | `verify_delivery.py` WARN |
| 辅助·WARN | scripts/output/file-index.md | 每文件 1 行反向索引登记（W### 段） | 版本行 WARN；结构健康由第 17 门禁 check_index_health.py 机器校验（段完整性/重复/最新段残留·FAIL 阻断） |
| 同步辅助·里程碑 | site 页脚 4 个（dukou-engine / index / cross-time-danmaku / tag-cloud） | 版本号 W### | `bump_version.py` 一键 |
| 同步辅助·里程碑 | 旁文档 1 份（.github/workflows/README.md） | 头部版本 + 里程碑行 | 手动（E2 8 项扫描） |

> 2026-08-08 强化：处置类任务（回退/删除/清理/审查）同样必须同步——CHANGELOG 对应 W 段补「处置收尾」字段，禁止因"非新功能"跳过。

### 11.2 禁止擅自修改的文件

> 误改后果列说明：违反会破坏历史事实 / 门禁一致性 / 版本控制，多数在 pre-commit 或 CI 层暴露，少数是隐性毒（如悄悄覆盖 memory 铁律）。

| 文件 / 目录 | 约束 | 误改后果 | 例外 |
|------------|------|---------|------|
| CHANGELOG.md **历史版本段**（W001-W423） | 历史事实不可改（E2 判据：历史条目保留旧值，只更新现役描述） | 历史审计链断裂：旧 W 段被改后 verify_delivery 范围/一致性检查误报或漏报，跨 session 无法追溯"当时做了什么" | 无 |
| CHANGELOG-ARCHIVE.md / file-index-archive.md / 交接文档-archive.md | 只追加新归档段，禁止回溯修改已归档内容 | 归档段被改 → 历史记录失真 + 范围漂移扫描误判；重跑归档脚本（如 _w417_rearch）会重复追加段 | 无 |
| .env | 含 API 密钥（sk-…），**禁止入库/commit**（gitignore） | 密钥泄露：push 后 GitHub 历史可被爬取，需吊销+轮换（W402/W412 先例），且 SECURITY-AUDIT 档会失效 | 本地可改（密钥轮换时） |
| SECURITY-AUDIT-*.7z + *.password | 敏感审计档，**禁止入库**（gitignore） | 审计档泄露 → 全部安全缺口公之于众，绕过审计闭环 | 仅本地解密查看 |
| node_modules / dist / __pycache__ / venv / .vscode | 依赖与构建产物，禁止入库（gitignore） | 仓库体积爆炸（数百 MB）+ 平台差异冲突 + CI 构建被本地产物污染 | 无 |
| scripts/output/rag_index.json（32MB） | 可重建索引，禁止入库 | 32MB 二进制入库 → 每次改动全量重传，仓库体积不可控 | 跑 scripts 重建 |
| scripts/output/screenshots / tests 基线 | 可重建产物，禁止入库 | 同上：体积 + 基线漂移（旧截图对比失真） | 重跑脚本重建 |
| scripts/verify_delivery.py / .git/hooks/pre-commit | 门禁本体，修改必须配套验证（py_compile + 全量跑通） | 门禁被弱化 → 文档漂移/范围漂移漏检，回到"声明≠落地"的假收敛（E1 铁律 31 次复现根因） | 经用户确认 |
| scripts/check_skills_index.py / check_index_health.py | 门禁本体（W498/W500 转正挂 verify），修改必须配套验证（负样本 + 全量跑通） | 门禁被弱化 → skills 索引 / 文档索引（file-index 段完整性·方法论 README 覆盖·编号上限）漂移漏检 | 经用户确认 |
| 文档规范.md 本身 | 元规则，修改须保持 §1-§10 一致性 | 规则自相矛盾 → 新 Agent 无所适从，同步/禁改边界失效 | 经用户确认 |
| user_profile.md / project_memory.md（memory） | 写入协议：Read-before-Edit + Grep-verify-after-Edit + 集中写入点（mem-wrap-up Step 5/7a） | 并发覆盖铁律段（W402 先例：E1 段覆盖 E2 扩展）；修复声明≠落地 → 跨 session 假收敛 | mem-wrap-up 收尾时 |
| assets/fonts/source/ | 字体源（W413 入库 +24MB），禁止删除/重命名 | 页面字体引用 404（站点字体回退系统字体）；删除后需重下 24MB 源文件 | 版本更新时经用户确认 |
| scripts/bump_version.py | 已知坑：**只改 W 号不改版本号主描述**，需手动补 vX.Y.Z（W413/W414/W415 三次同模式）；主描述正则已修复跨括号吞内容（W417）；W500 起已支持项目说明次级版本行（`- **当前版本**：` 格式·仅换版本号不追加 W） | 版本号与 W 号错位 → verify_delivery 版本行检查 FAIL，或主描述残留旧 W | 手动补版本号 |

### 11.3 接手速查（新 session / 新 Agent 首读）

1. 读 交接文档.md「零、当前阻塞」+「一、当前进度」→ 定位 HEAD 与下一 W 编号
2. 读 本节 11.1 → 明确本次 W 完成后必须同步的文件清单
3. 读 本节 11.2 → 明确禁止擅自修改的文件（历史段/密钥/门禁/归档）
4. 动手前查单一事实源（§3）：想写的内容归属哪个文件？不重复写
5. 提交前双重门禁：任务清单（TaskCreate 系列）逐项勾选 11.1 + pre-commit 自动跑 `verify_delivery.py`；临时跳过用 `git commit --no-verify`（不推荐）
6. 每个文件修改后：Grep spot-check 验证落地（E1 升级版铁律·声明≠落地）

### 11.4 同步核对速查表（W### 完成后逐项勾选）

> 将 11.1 的 8 类 + 验证/收尾合并为一张可勾选清单，新 Agent 提交前逐项打勾（对应门禁 1·任务清单）。
> 勾选完成 + pre-commit 跑 `verify_delivery.py` 全绿后，方可 commit。

| # | 文件 | 必做动作（W### 为本次编号） | 完成 |
|---|------|---------------------------|------|
| 1 | CHANGELOG.md | 新增 vX.Y.Z W### 段（来源/执行/验证/状态四件套）+ 编号规则 W001-W### | ☐ |
| 2 | 交接文档.md | 阻塞段 HEAD 引用 + 当前进度段里程碑 + 版本号列表 + 接续编号（当前 W###·下一 W###+1） | ☐ |
| 3 | README.md | 版本行 vX.Y.Z W###（**注意：版本行 + 开发者区 + 双索引三处 W 范围都要查**） | ☐ |
| 4 | STRUCTURE.md | 版本行（含主描述，`—` 分隔格式 bump 不自动改，需人工核） | ☐ |
| 5 | docs/00-导读/项目说明.md | 版本行**两处**（头部引用行 + 第 47 行「当前版本」列表行·E2 深处残留教训） | ☐ |
| 6 | scripts/output/file-index.md | 新增 W### 段登记（每文件 1 行，含改动摘要） | ☐ |
| 7 | site 页脚 4 个 | dukou-engine（长链人工插入）/ index / cross-time-danmaku / tag-cloud（后 3 个 bump 自动） | ☐ |
| 8 | 旁文档 1 份 | .github/workflows/README.md（头部版本 + 里程碑行） | ☐ |
| 9 | verify_delivery.py | 全绿：六文档含页脚 v/W（核心 2 硬门禁 CHANGELOG + 交接文档 · 辅助 4 WARN README/STRUCTURE/项目说明/file-index）+ 无范围漂移 + A4 "209 篇" + A1-A6 磁盘计数==README 声明（当前 615==615） | ☐ |
| 10 | commit + push + CI | commit（pre-commit 门禁自动跑）→ push → CI/Security/Deploy/Screenshot 全绿 → 状态行收尾 commit | ☐ |

> 处置类任务（回退/删除/清理/审查）同样走本表：CHANGELOG W### 段补「处置收尾」字段，禁止跳过。

### 11.5 治理文档维护契约（防追加式乱写 · 2026-08-18）

> 以下 5 份治理文档均已在**文件头部内嵌「维护契约」**（权威源在文件本身），本节是登记索引。
> 共同规则：保持既有骨架 / 就地替换旧表述 / 全篇去重 / 禁止追加速记类历史段 / 版本与 HEAD 核对一致。

| 文件 | 骨架约束 | 特有规则 |
|------|---------|---------|
| 新Agent启动Prompt.md | 【正文】五段骨架（定位现状/文件管控/结构与历史/干活铁律/现役口径） | 是可直接发送的 payload：禁止追加「W### 速记/治理要点」段；口径只保留一份现役值 |
| 交接文档.md | 「零/一/二/三」四段骨架 | 「最后更新」行只写最新 1–3 批（每批 ≤1 行），禁止堆历史；HEAD 表述必须与 `git rev-parse --short HEAD` 一致 |
| AGENTS.md | §1–§8 骨架 | 新铁律就地并入 §6，禁止尾部速记段；脚注版本须与 HEAD 一致 |
| STRUCTURE.md | 目录树 + 说明表 | 只在目录/命名/协作约定变更时修改；版本行由 `bump_version.py` 维护 |
| CHANGELOG.md | 每段四件套（来源/文件/验证/状态） | 历史段禁改；新段用脚本插入；建议单段 ≤25 行；编号 max+1 |
| DESIGN.md | §1–§11 编号体系（含子节/字母后缀） | 新增体系用子节（如 §4A），禁止打乱既有编号；规范变更就地修订并更新 §11 相关文件；批次引用只作来源注记 |
| README.md | 版本行 + 用户分区 | 版本行归 bump；禁止追加里程碑/批次速记；计数声明与 verify_delivery 一致 |
| docs/00-导读/项目说明.md | 版本行两处 + 待办区 | 状态/待办以交接文档为准；禁止追加速记；版本两处人工核对 |
| scripts/output/file-index.md | 按 W 追加登记 | 每文件 1 行 + 一句摘要；历史段禁改；先有 CHANGELOG 段再登记（双索引铁律） |
| docs/10-方法论沉淀/README.md | 目录索引 | 新增方法论须登记并与交接文档「三」同步；「当前版本」行是目录快照非项目版本 |

> 违规后果：追加式更新会重现 W419–W424 时期的「启动 Prompt/交接文档膨胀 + 口径新旧并存」问题；发现此类改动应就地重构为契约格式后再提交。
