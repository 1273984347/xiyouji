# 《详解西游记》项目 · 交接与维护参考手册

> 本手册由 `交接文档.md` / `README.md` / `CHANGELOG.md` / `STRUCTURE.md` / `DESIGN.md` / `CONTRIBUTING.md` / `mcp-server/README.md` / `docs/00-导读/项目说明.md` 及 git 历史汇总提炼，供后续开发与维护快速上手。
> 提取时间：2026-08-05 · 项目当前版本 **v2.3.8（2026-08-04，W357）** · 下一 W 编号 **W358**。

---

## 1. 项目背景

| 项 | 内容 |
|---|---|
| 项目名 | 详解西游记（xiyouji） |
| 定位 | 混合型《西游记》解读项目：既写给深读原著的读者，也写给只取一瓢的过路人 |
| 创建日期 | 2026-07-21 |
| 核心理念 | 「一源多形」：原文本层 → 解读层 → 可视化层 → 数据层 |
| 写作模式 | AI 代理协作（主代理派发 + subagent 并行产出 + 主代理 spot-check 复核），以 `W###` 编号串联双索引 |
| 当前规模 | **629 篇研究文档**（A1-A6）+ **85 个可视化/交互页** + **42 个结构化 JSON 数据集**（dataset/）+ 全站设计系统 + RAG 对话 + 数据 API + PWA 离线 + 英文站（site/en/ 51 文件） |
| 战略判断 | 当前已是"静态博物馆"——内容模板化扩容边际收益趋零，**重心转向工程化（RAG/数据 API/图形探索/发布）而非继续加篇目** |

**内容分层（docs/ 十大板块）**
- 00-导读（项目说明/术语表/文档规范）· 01-全书逐回解读（100 回，已收束）· 02-人物深度分析（199 篇）· 03-主题与情节专题（201 篇）· 04-文化与历史背景（20 篇）· 05-诗词歌赋 · 06-个人随笔（44 篇）· 07-学以致用 · 08-提升认知 · 09-精神塑造 · 10-方法论沉淀（6 篇）
- 轨标四选一：大众普及 / 学术研究 / 教学讲解 / 个人创作（每篇开头标注）

---

## 2. 架构说明

### 2.1 四层数据架构
```
source/  ──原著文本/引用──►  docs/  ──Markdown 解读──►  site/  ──HTML 站点(D3.js)──►  用户浏览
   ▲                              ▲                          │
   └──── scripts/ ────────────────┘                          ▼
        Python 文本分析 ──产出──► dataset/*.json ──桥接──►  site/data/ 可视化页(EMBEDDED_DATA)
```
- **静态优先**：所有 `site/data/*.html` 支持 `file://` 直接打开，数据内嵌（`EMBEDDED_DATA` fallback），唯一外部依赖是 D3.js v7 CDN。
- **可在线增强**：可选本地服务（数据 API / RAG 后端）在线时提供检索与图谱，离线自动降级。

### 2.2 关键子系统
| 子系统 | 入口 / 文件 | 说明 |
|---|---|---|
| 静态站点 | `site/index.html` `dashboard.html` `mobile-index.html` | 新中式·数字雅集设计（W334），system.css + tokens.css 设计系统 |
| 可视化页 | `site/data/*.html`（85 页） | A-AH 34 类 + Q+/Q++/Q+++ + 全文检索 + 图谱探索器等 |
| 数据 API | `scripts/api/api_server.py`（默认 127.0.0.1:8787） | 零依赖 stdlib ThreadingHTTPServer；`/datasets` `/dataset/<name>` `/search?q=` `/graph` `/graph/<name>` `/health` `/openapi.json` |
| 本地 RAG | `scripts/rag/`（rag_server.py 默认 127.0.0.1:8777） | LightRAG 架构零依赖落地（BM25 + 图谱层）；`LLM_API_KEY` 存在时走真实生成 |
| 渡口问津 | `site/dukou-engine.html` + `site/static/js/rag-chat.js` | 前端对话组件，调用 RAG `/query`、`/graph`；离线回退模板引擎 |
| 知识图谱 | `site/data/graph-explorer.html` | 纯 SVG 力导向，多图切换（佛学=AI=西游三元映射 / 取经人物关系） |
| MCP 工具 | `mcp-server/xiyouji_mcp.py` | 5 个只读工具（drl_spotcheck/data_validate/docs_index/lint_links/a11y_audit） |
| CI/CD | `.github/workflows/`（4-5 个 workflow） | screenshots + lighthouse + a11y + security + perf；全 CI 已转绿 |
| 工程脚本 | `scripts/`（Python + JS） | 文本分析 34 类（A-AH）+ 截图审查 + 校验 + 构建桥接脚本 |

### 2.3 双索引可追溯机制（铁律）
- **正向索引**：`CHANGELOG.md`，按 W### 段记录变更（四件套：来源/文件/验证/状态）
- **反向索引**：`scripts/output/file-index.md`，按文件查被哪些 W### 修改过
- 每个 W### 与每个改动文件必须**同时**登记两个索引，不可只改其一

---

## 3. 模块职责

### 3.1 目录职责
| 路径 | 职责 |
|---|---|
| `docs/` | Markdown 解读主体（十大板块，命名规范 `第NNN回-*.md` / `主题.md`） |
| `source/` | 原文本（`分回/` 100 回原文 + `shendu/` 100 篇深读切片）+ 引用（学术论文索引 50 条 / 网络解读 15 条） |
| `site/` | 静态站点：`index`/`dashboard`/`mobile-index` + `chapters`/`characters`/`themes`/`data` + `static/`(css/js/fonts/images) + `en/`(英文站) |
| `scripts/` | Python 文本分析（A-AH 34 类）+ 工具（截图/校验/桥接）+ `api/` + `rag/` + `output/`(产物与索引) |
| `timeline/` | 取经路线 / 大事年表 / 人物时间线 |
| `assets/` `references/` `tools/` | 图片字体占位 / 参考文献 / 辅助工具（章节切分） |
| `mcp-server/` | 工程化只读 MCP 工具封装 |

### 3.2 数据可视化 34 类（A-AH）映射
- **A-L 基础**（12 类）：文本/人物/情节/关系/地理/时间/哲学/风险/妖怪社会学/权力/轮回/商业
- **M-U 扩展**（9 类）：洞府/法术/美学/音乐/源流(Q)/批评史(Q+)/弹幕(Q++)/解构/全球/反事实/错位
- **V-AH 跨学科**（13 类）：职场/社媒/游戏网文/伦理/图表设计/叙事实验/视觉艺术/方法论矩阵/认知心理/生态学/法理经济/物质考古/语言学

### 3.3 工程化方向（E1-E8，已全部完成）
CI/CD 矩阵 · a11y 40 条 WCAG 2.2 规则 · 性能优化(perf_optimize.py) · i18n 英文站 · Playwright E2E 三层测试 · Core Web Vitals RUM · SEO(JSON-LD/sitemap) · 安全加固(CSP/SRI/5 安全头)

---

## 4. 待办事项

### 4.1 短期（接手即可推进）
- [x] A2 随笔扩容（43 篇，Batch 1-10 收束）
- [x] A3 人物深化（199 篇）· A4 主题专题（201 篇，已冻结，仅允非模板化 gap-fill）
- [x] A5 八层明代镜像闭环 · A6 诗词中英文专题全覆盖
- [x] 数据闭环（81-hardships.json 由半空变闭环）· RAG 前端接入 · 数据 API 化 · PWA · 图谱探索器
- [ ] **将增强版截图审查纳入迭代发布流程**（内容/样式大幅改动后重跑 `npm run screenshots`）
- [ ] **RAG 真实生成**（补齐 `answer()` LLM 生成路径 + 可选 embedding 向量检索，需 LLM key）
- [ ] **发布与可发现性**：git push + 部署（GitHub Pages / CloudStudio）+ SEO/sitemap + 英文站扩关键新页

### 4.2 中期 / 长期
- V2 新增可视化维度（3D 地理 / 动态关系网络）· E1 截图审查 CI 化
- S2 关键随笔/专题发布（博客/公众号/知乎）· S4 学术投稿（基于 50 条学术论文索引）
- **战略提醒**：内容模板化扩容边际收益已趋零，优先做"能问能搜"的活系统，而非再加静态篇目

### 4.3 已冻结 / 收敛项
- A3（199 篇）、A4（201 篇，W342 后仅允填补长期引用空缺的非模板化篇目）已冻结
- A6 中英文专题均无剩余 gap；S3 方法论外部分享 13 篇已全部发布收束

---

## 5. 已知问题与注意事项（接手必读雷区）

| # | 雷区 | 规则 / 处置 |
|---|---|---|
| 1 | **D3.js `file://` 限制** | 禁止 `fetch()` 跨域 JSON；必须 `EMBEDDED_DATA fallback`；改数据须同步两处（内嵌对象 + `scripts/output/data/*.json`） |
| 2 | **Playwright + Node 18** | `npm install` 后需 Node ≥18 才能跑 `npm run screenshots`；postinstall 自动下载 chromium(~120MB) |
| 3 | **EMBEDDED_DATA fallback 日志** | `console.warn('[INFO] 使用嵌入数据')` 是设计预期，**勿"修复"为 error** |
| 4 | **截图切片目录** | `slice_screenshots.py` 每次自动清旧切片；`.gitignore` 已忽略 `screenshots/slices/` |
| 5 | **双索引铁律** | 每文件 + 每 W### 必须同步 CHANGELOG 与 file-index，不可只改其一 |
| 6 | **A4 七段式模板** | 范围界定/叙事结构/主题深度/文化隐喻/影视改编/延伸思考，不可省略 |
| 7 | **D3 v7 API** | 条件样式用 `.each()` 模式；`.text()` 防 XSS；`.html()` 仅可信 EMBEDDED_DATA；force simulation 须 `.stop()` 或设上限 |
| 8 | **CSS token** | 27 个设计变量集中在 `:root`/tokens.css；Hero 渐变固定值；改配色须全页同步 |
| 9 | **响应式断点** | `768px`（平板）与 `520px`（窄屏）必须通过视觉验证 |
| 10 | **a11y 键盘可达** | div 交互元素须 `role="button" tabindex="0"` + 键盘事件；`aria-*` 必填 |
| 11 | **空数据保护** | 渲染函数须以 `if (!data || data.length === 0)` 空值守卫开头 |
| 12 | **跨 session E1 铁律** | 每次 W### 落地后 Grep spot-check 验证 memory/索引是否真正包含对应段（防 memory 层假收敛） |

**历史遗留已知问题（已修复态，留意回归）**
- 第 069/099 回曾混入网页垃圾内容，W286c 已清除；第 099 回已补 SD075 深度解读
- `site/data/character-relationship-3d.html` 曾因误引 d3 死代码导致整页不渲染，W334 已修复
- `sync_docs.py` 曾用 `eval()` 触发 Security 门禁，W343 改为安全解析器

---

## 6. 环境配置与部署流程

### 6.1 系统要求
| 依赖 | 最低版本 | 用途 |
|---|---|---|
| Python | 3.8+（实测 3.12/3.13 可用） | 文本分析（jieba + Pillow）、数据 API、RAG |
| Node.js | 18+（硬性） | Playwright 截图审查、前端构建 |
| Git | 2.20+ | 版本控制 |
| 浏览器 | 任意现代浏览器 | 打开 `site/`（支持 `file://`） |

### 6.2 一键复现
```bash
git clone <repo-url> xiyouji && cd xiyouji
cd scripts && pip install -r requirements.txt      # Python 依赖
npm install                                        # Node 依赖 + 自动 playwright install chromium
# 验证：双击 site/data/81-hardships.html 应渲染 D3 图表
npm run screenshots                                # 批量截图 + 布局断言 + 切片
python check_all_js_syntax.py                      # 内联 JS 语法检查
python detect_unwrapped_tables.py                  # 表格溢出扫描
```

### 6.3 常用命令
| 命令 | 用途 |
|---|---|
| `npm run screenshots` | Playwright 全页截图 + 布局断言 + 800px 切片 |
| `npm run test:smoke` / `test:deep` / `test:visual` | E2E 三层测试（冒烟/深度/视觉回归） |
| `python check_all_js_syntax.py` | 批量检查内联 JS 语法 |
| `python detect_unwrapped_tables.py` | 扫描未包裹横向滚动的 `<table>` |
| `scripts/api/api_server.py` | 启动数据 API（127.0.0.1:8787） |
| `scripts/rag/rag_server.py` | 启动 RAG 后端（127.0.0.1:8777，可选 LLM key） |
| `python scripts/docs_index.py` | 重新生成 docs/ 索引 |
| `scripts/pre-commit-validate.py` | pre-commit 门禁（文件计数声明校验） |

### 6.4 可选配置（`.env.example` / `.env.lightrag.example`）
- `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` / `EMBEDDING_MODEL`：启用 RAG 真实生成（无 key 时走零依赖 BM25 + 图谱召回）
- Neo4j 升级路径见 `scripts/rag/README.md` 与 `graph_seed_neo4j.py`（对齐 LightRAG Neo4j 后端）

### 6.5 部署
- **静态站点无需服务器**：直接交付 `site/` 目录即可 `file://` 浏览；部署 GitHub Pages / CloudStudio 时上传整个 `site/` + `dataset/`
- **CI**：`.github/workflows/` 已含 4-5 个 workflow（screenshots / lighthouse / a11y / security / perf），提交即触发，全 CI 当前为绿

---

## 7. 联系人及负责人

| 角色 | 身份 |
|---|---|
| 项目所有者 / 提交者 | GitHub 账户 **1273984347**（`<1273984347@users.noreply.github.com>`），全部提交作者 |
| 代码仓库 | `https://github.com/1273984347/xiyouji.git`（origin，main 分支，已 push 至 W357） |
| 协作模式 | 单人 + AI 代理（主代理 + subagent），无记录的多人分工；文档未登记具体真人姓名/邮箱 |
| 方法论权威源 | `docs/10-方法论沉淀/`（DRL 真循环 / 三 skill 闭环 / E1 铁律 / Preflight 与 Subagent 模板 / 双索引可追溯改造） |
| 三 skill 闭环位置 | `C:\Users\12739\.trae-cn\skills\{deep-review-loop, mem-wrap-up, self-evolution}\SKILL.md` |
| 外部服务/敏感操作 | 发布、API 改动、社交发文等需先确认；删除前必确认，优先回收站 |

> 注：文件内未记录具体联系人姓名与电话。如需对接，以 GitHub 仓库 owner（1273984347）为唯一可追溯的责任主体；部署/发布动作请通过该账户执行或经其授权。

---

## 8. 跨 session 接续流程（速查）
1. 新 session 先读：`交接文档.md` + `README.md` + `docs/00-导读/项目说明.md`
2. 接续 W 编号：当前 W357，下一 **W358**
3. 选方向：见「二、下一步方向候选清单」（按战略/内容/可视化/工程化/分享五类）
4. 遵循方法论：见「三、方法论沉淀」（Preflight 双轨验证 / E1 铁律 / 双索引 / Subagent 盲信不可信）
5. 完成后更新：本交接文档「一、当前进度」+「三、方法论沉淀」+「四、待办」+ 六文档同步（CHANGELOG/README/STRUCTURE/项目说明/file-index/交接文档）

---
*本手册为信息汇总，细节以各源文件为准。关键源文件：交接文档.md · CHANGELOG.md · STRUCTURE.md · DESIGN.md · README.md · docs/00-导读/项目说明.md · mcp-server/README.md*
