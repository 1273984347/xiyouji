# 交接文档

> 轨标：教学讲解
>
> 本文件用于跨 session 交接，记录当前进度、下一步可推进方向、方法论沉淀与待办事项。
> 创建于 2026-07-25（v2.0.41 W068 后）
> 最后更新：2026-08-31（v2.3.134 W535 决策闸门取数自动化：fetch_gate_stats.py 新建——GoatCounter API v0 拉近 7/30 日 UV·口径取自官方 OpenAPI 实拉核对·页面 UV=total−total_events·负样本自测 14/14 + fixture 离线端到端·读者数据复盘.md 新增「第零、UV 取数方式」段；遗留=用户在后台生成 API 令牌写入 .env 即可一键判定）·2026-08-30（v2.3.133 W534 治理文档递增数字字面量修复：交接文档「接续 W 编号」条写死「当前 W531·下一 W532」改引用式表述——W520「递增数字禁字面量」规则外存量盲区 1 处治愈·六份治理文档全量复扫 0 残留）·2026-08-29（v2.3.132 W533 skills 归属策略显式化 + 坑④入 playbook：agent-session-loop / deep-review-loop / mem-wrap-up / self-evolution 唯一 master 定为全局安装版、本仓库副本降为受控只读镜像（MIRROR_SKILLS 强制禁 --sync/--force，--self-test 5/5 + 真数据反证）+ 坑④写入 version-bump v1.2.0）·（W532 及更早批次为历史，按维护契约②只留最新 1–3 批，详见 [CHANGELOG.md](CHANGELOG.md)；v2.3.134 W535 起生效）
> 与 [CHANGELOG.md](CHANGELOG.md)（变更日志）/ [STRUCTURE.md](STRUCTURE.md)（目录结构）/ [README.md](README.md)（项目说明）/ [docs/00-导读/项目说明.md](docs/00-导读/项目说明.md)（项目状态）配套使用。
>
> **维护契约（防乱写）**：本文件是项目进度中枢，只允许结构性编辑——① 保持「零 / 一 / 二 / 三」四段骨架不变；②「最后更新」行只写最新 1–3 批摘要（每批 ≤ 1 行），**禁止把历史批次堆进该行**（历史已归档 / 在 CHANGELOG）；③ 每批详细记录只写 CHANGELOG + 归档（历史段禁改）；④ 任何「当前 HEAD / 当前版本」表述必须与 `git rev-parse --short HEAD` 核对后更新；⑤ 本文件是状态快照，与 CHANGELOG（时间线）冲突时以 CHANGELOG 为准。
>
> **历史归档**：v2.0.39-v2.2.49（W066-W280）段的详细进度记录已迁移至 [交接文档-archive.md](交接文档-archive.md)。本文件仅保留 v2.2.50+（W277）现役记录与全局概要。

---

## ⚠️ 零、当前阻塞（接手必读）

**原唯一长期阻塞「档 B 真实生成需 LLM_API_KEY」已于 W402 解除**（provider 化 Base URL·渡口问津生成式问答·LLM_API_KEY 配于本地 .env 已 gitignore）。**访问数据基线已接入（W403·localStorage 自建·仅本浏览器可见）**，**S2 分发完成（W404 精选 27 篇发布版 + 合集页 curated.html + W405 第二批 27 篇随笔发布版·44 篇随笔全发布）**，**GoatCounter 已接入并完成自托管修复（W425 注册接入 + W426 gc.zgo.at 大陆 DNS 污染 → count.js 本地自托管·计数回传 goatcounter.com·线上实测 HTTP 200）**；**真实跨访客读者量仍待回传验证**（部署生效后观察后台数据）；有流量则继续分发（S4 学术投稿等），无流量则停止内容生产。另存在平行入口 `xiyouji-agent-web`（基于 CodeBuddy Agent SDK·用 `CODEBUDDY_API_KEY`·默认 `PROJECT_CWD=D:/1/xiyouji` 直接对话/检索/跑脚本），与 scripts/rag 档B 二选一或并行。**安全审计 P0-1/P1-1 已处置（W411）**：Web Agent 默认权限改 acceptEdits + 可选 AGENT_WEB_TOKEN 认证 + 权限/工作目录白名单 + 仅回环监听 127.0.0.1·MCP 4 工具路径白名单拒越界。**安全审计剩余项已处置（W412）**：P0-2 禁运行时覆盖 CODEBUDDY_API_KEY/BASE_URL（前端表单移除·仅服务端 .env）·P1-2 静态站 XSS 转义 6 页·P2 RAG 参数钳制/LLM 端点校验/SSE 超时与断开清理/DOMPurify 消毒/依赖锁定·P3 日志收敛/CORS 白名单/死代码/版本漂移；密钥吊销轮换需用户在 DeepSeek 控制台操作（P1-3 仅告知）。**W424 对抗性审查修正与全仓整理已处置**：A4 门禁校准 209（verify EXPECT_A4 201→209）·3D 页 defer 修复·EN 页 Ch. 腐蚀修复·security 依赖审计修复（只扫仓库 requirements.txt·high 103→0）·新Agent启动Prompt/workflows README/DESIGN 同步·全仓清理（截图 416MB + 过期报告 14 + 原始审计 JSON 4）·RAG 索引重建。**W424 SRI/CSP 已处置**：95 外部脚本标签 SRI（SHA-384）·159 页 meta CSP（680 内联脚本哈希·script-src-attr none）·EN 腐蚀第二波（""X"" 32 处 + 反引号 4 处）·6 页 sankey 漏引修复·复盘沉淀批次（腐蚀/插件引用门禁 + 方法论新增 + 内容同步）。**W417 文档健康治理已处置**：CHANGELOG/file-index/交接文档归档精简降达标·verify_delivery 新增 A1-A6 真实计数校验（611==611）·actions 升级消除 Node 20 deprecation·RAG 索引重建演练通过·bump_version 增强（--desc/W 范围锚点/页脚同步）·LICENSE 双协议边界补强·sitemap 补全 154 页。**W418 内容质量深化已处置**：全站死链巡检（site/en/ 29 broken 链接修复——EN 版存在指向同目录/无 EN 版回退中文原版 ../data/*.html 加 lang="zh-CN" 标注·docs/ 全量 4784 链接 0 broken）+ A1 逐回解读 23 回补 `> 导航：` 引用行（100 回导航全覆盖 100/100）。**W419 修复 A1 深度解读 SD 错位已处置**：22 篇 SD（SD038-052/056-062）编号≠真实回号归位（根因 W286 合并脚本只读源第一行·元数据在第三行→fallback 按编号放置）·按正文真实回号范围式复制 40-72 回全覆盖（原 63-72 十连回空白消除）·源文件 24 篇元数据/H1/正文内嵌/关联行修正·第 56 回补写 SD101。**W420 A1 内容质量深化已处置**：A1 深度解读 100/100 补全（第038/039回新增 SD102/SD103·source 切片 101→103）+ 56 回「对应原著/数据指标」结构化元数据补齐 + 99 回导航上一回/下一回错链修复（此前约 60 回指向非相邻回）+ 10 回 sd-crossref 关联块死链修复（66 处·docs/01 1715 链接 0 broken）。**W421 Screenshot Review 提速优化已处置**：screenshot-review.yml 新增改动范围判定（页脚/文档-only 跳过·site/data 变更定向截图·static/脚本/workflow 变更全量·schedule/dispatch 全量）+ batch_screenshots.js --only-pages 定向截图 + Playwright 浏览器缓存 + checkout fetch-depth 0。**W422 全量治理已处置**：perf.yml 补 push 触发（LHCI 预算门禁首次在真实发布路径运行）·verify_delivery 新增 4 项门禁（A1 导航相邻性/docs/01 链接/sitemap 覆盖/site/data 回退模式）·文档健康归档（CHANGELOG 83 行·file-index 127 行·交接文档 556 行）·双索引规则校准（新文档执行 + 存量以 file-index 追溯）·README 命令修正 + JS 检查进 CI·认知总览计数校准（756/611/211/209）·Dependabot/mypy report-only/a11y 口径/artifact 瘦身/_DEBRIS 清理。**W459 V2 审查收尾已处置**：D2 回目跳转死链修复（方案错误约定 `第NNN回-回目摘要.md` → A1_DOC_MAP 内嵌 100 条真实文件名映射·路径深度 ../→../../）+ check_dynamic_links.py 动态链接门禁挂 verify_delivery（补 lint_links 静态盲区·首跑抓出 tag-cloud dashboard 死链 + EN source_doc 虚构路径并同批修复）+ V2 方案文档回写 + 首页精选补 geo-3d 入口；W456-W458 积压改动同批基线提交（de8047a）。当前 HEAD = v2.3.134 W535（决策闸门取数自动化——fetch_gate_stats.py 新建·GoatCounter API 拉 UV·自测 14/14；详见 CHANGELOG）。**W530 遗留待办已部分解除（W535）**：取数环节不再需要人工登后台——新增 `scripts/fetch_gate_stats.py`（零第三方依赖·GoatCounter API v0 拉近 7/30 日页面 UV·`--self-test` 负样本 14/14 + `--fixture` 离线端到端），仅剩「生成 API 令牌写入 `.env`」一次性动作，随后即可跑 `judge_gate.py` 正式判定。

- 历史里程碑（W343-W358·v2.2.9x-v2.3.9）的详细提交记录已归档至 [交接文档-archive.md](交接文档-archive.md)（W343-W358 段）；W066-W280 段此前已归档。

---

## 一、当前进度（v2.3.134 W535 决策闸门取数自动化（fetch_gate_stats.py·GoatCounter API v0 拉近 7/30 日 UV·负样本自测 14/14） + v2.3.133 W534 治理文档递增数字字面量修复（交接文档「接续 W 编号」改引用式·W520 规则外存量 1 处） + v2.3.132 W533 skills 归属策略显式化 + 坑④入 playbook（agent-session-loop / deep-review-loop / mem-wrap-up / self-evolution 全局为 master·仓库为受控镜像·version-bump v1.2.0 收录坑④）·（更早批次详见 CHANGELOG.md））

### 已完成里程碑概要

- **v2.3.134 W535 决策闸门取数自动化（2026-08-31）**：
  - 新建 `scripts/fetch_gate_stats.py`（stdlib 零依赖）：GoatCounter API v0 `stats/total` 拉近 7 / 30 日独立访客，输出 judge_gate.py 可直接消费的 `--uv7/--uv30`；口径取自官方 OpenAPI 当批实拉核对——**页面 UV = total − total_events**，时间窗为含今天的闭区间
  - 验证：`--self-test` 14/14（缺字段/事件数倒挂/401/403/缺令牌等负样本全抓到）+ `--fixture` 离线端到端 UV 计算正确 + 真实路径无令牌 exit 2 给可操作提示
  - `读者数据复盘.md` 新增「第零、UV 取数方式」（一次性令牌准备 + 两条命令）；遗留 = 用户在后台生成 API 令牌写入 `.env`（gitignore）后即可一键判定
  - 坑⑤适用面扩写（version-bump v1.5.0）：Git Bash 给 Windows 原生程序传路径（含 Python `--fixture` 实参）一律 `C:/` 形态

- **v2.3.133 W534 治理文档递增数字字面量修复（2026-08-30）**：
  - 漂移点：交接文档「九、使用说明」第 2 条「接续 W 编号」写死「当前 W531·下一 W532」——W520 立「递增数字禁字面量」规则时只治愈 skills 与 README/STRUCTURE/项目说明三类载体，治理文档的例行说明段未纳入扫描范围，滞后 2 批且随每批发版持续漂移
  - 处置：改引用式表述——接续编号以本文件「一、当前进度」段现役值为准、新批编号按 CHANGELOG 顶部「W### 编号规则」取现役段 max+1，**本文件禁止内嵌具体 W 号**
  - 误报更正：本批接手时曾报「dukou-engine 页脚链首滞后 W529」，复核为**误判**——取链首误用 `tail`（链尾为最旧条目）；实测链首 `v2.3.132 W533` 与现役一致，页脚零滞后
  - 验证：六份治理文档全量复扫 `当前 W###` / `下一 W###` 字面量 0 命中；页脚链首 Grep 复核；verify_delivery 全绿

- **v2.3.132 W533 skills 归属策略显式化 + 坑④入 playbook（2026-08-29）**：
  - 归属定案：agent-session-loop / deep-review-loop / mem-wrap-up / self-evolution 唯一 master = 全局安装版；本仓库 skills/ 副本改为受控只读镜像（保留它是为满足「共享机制须 git tracked」铁律——作品仓库无版本控制），方向单向 全局→仓库
  - sync_skills.py 落 MIRROR_SKILLS + sync_blocked()：镜像技能 --sync 与 --force 双禁（真数据反证：脏写仓库后 --force 仍 0 更新、master md5 前后不变），--self-test 5/5
  - xiyouji-version-bump v1.2.0：坑④（bump 原地替换三简单页脚链首 v/W、描述滞留上一批且吞历史条目）从个人记忆上移到仓库 playbook，供跨 session Agent 读取
- **v2.3.131 W532 交接文档最后更新滚动链裁剪回契约上限（2026-08-29）**：
  - 交接文档「最后更新」滚动链由 9 批堆叠裁回契约上限 3 批（W531/W530/W529），删 6 批前逐批 assert CHANGELOG 对应段存在——信息零丢失；行内加「历史见 CHANGELOG」指针
  - 触发因由：W531 部署状态全查暴露该文件自身维护契约②（只留 1–3 批）长期被违反，历批只 prepend 不收尾
- **v2.3.130 W531 skills 部署全查 + 三真源降级保护（2026-08-29）**：
  - `scripts/sync_skills.py` 重写比对层：`judge_direction()` 按 frontmatter 版本号（其次 mtime 弱证据）判漂移方向，`--sync` 对「全局更新/同版冲突/方向不可判」默认拒绝降级（`--force` 越权），新增 `--take-global` 反向回写与 `--self-test` 4 负样本；比对统一 CRLF→LF 归一化（根除 autocrlf 假漂移）
  - 4 个通用技能回写为全局真源版（1.2.0 / 1.4.0 / 1.3.0 / 1.2.0-qwenwork-native，7 文件）；回写后 `--check` 无漂移、三处真源（作品仓库 / 全局安装版 / 本仓库）内容收敛
  - 附带发现：`ai-contest-work-descriptor` 未部署全局（本会话补装验证触发，作品仓库 git 纳管用户选暂不处理）；4 通用技能双仓库并存的双真源归属约定待后续批次

> 历史概要（W531 及更早）的详细记录见 [CHANGELOG.md](CHANGELOG.md)（W534 裁剪：里程碑概要按维护契约只留最近 5 版，删前逐批断言 CHANGELOG 有段·信息零丢失）。

## 二、下一步方向候选清单

> 按"内容扩容 / 可视化深化 / 工程化 / 分享发布 / 战略转型"五类列出。

### 优先级零：战略方向（来源：references/例-佛法AI框架原始对话.txt 中的产品分析）

> 以下判断来自对项目实际状态的审计，非模板化扩容建议。

**核心问题（曾为唯一阻塞·W402 已解除）**：项目曾是"静态博物馆"——86 个可视化页面数据硬编码，RAG 后端（scripts/rag/）虽已前端接入（W336 渡口问津 + W358 修复 draft 主回答渲染 + 多轮 history 接缝），但**真实 LLM 生成路径曾未接通**（answer() 仅有 BM25+图谱检索占位，档 B 待 LLM_API_KEY）——**已于 W402 解除**（provider 化 Base URL·LLM_API_KEY 配于本地 .env 即接通真实生成，key 存在即生成/无 key 回退模板）。另有平行入口 `xiyouji-agent-web`（Web Agent「西游记·渡口问津」·CodeBuddy Agent SDK·`CODEBUDDY_API_KEY`·PROJECT_CWD 指向本项目）可绕过 scripts/rag 直接对话/检索 docs/ + 跑 scripts/ + 写 dataset/——两条"能问"路径可并行。内容扩容（A3/A4 继续加篇）的边际收益已趋近于零。

**建议方向（按优先级）**：

1. ~~**提交存量**~~：✅ 已完成（W321-W334 全部 commit，2026-08-03）
2. ~~**RAG 前端接入**~~：✅ 已完成（W336·site/static/js/rag-chat.js「渡口问津」·index+dashboard 已嵌入·端到端验证通过）
3. ~~**数据产品化**~~：✅ 已完成（W336·dataset/ 40 个 JSON + README 数据手册·scripts/extract_datasets.js）
4. **停止模板化扩容**（策略决定）：A3 215 篇已冻结；A4 原冻结 199 篇，**用户于 W342 放宽冻结例外**：允许填补长期引用空缺的非模板化篇目（权力五联对照 W084·自 W089 起即以 W084 编号互链却长期未成稿 + 妖怪身份政治 W342 身份政治总论），A4 现 209 篇。不再按"四位理论家+七段式"模板生产新篇目。

**下一阶段方向（W340+）**：

- ~~**RAG 质量提升**~~：✅ 已完成（W337·xiyouji_rag.py 重写·RRF 四路融合+专名词典+字段加权）
- ~~**数据 API 化**~~：✅ 已完成（W337·scripts/api/api_server.py·41 数据集+跨集检索+/graph）
- ~~**可视化交互深化**~~：✅ 已落地（W337 vis-tools + W338 范式复用·search/dashboard 数据中枢）
- ~~**移动端 PWA**~~：✅ 已完成（W337·manifest+sw+图标）
- ~~**知识图谱探索器**~~：✅ 已完成（W339·graph-explorer.html 纯SVG力导向+多图切换+/graph 端点+dashboard 接入）
- ~~**图谱关系语义增强**~~：✅ 已完成（W340·边按关系类型着色+语义权重粗细+关系类型筛选+钻取面板「语义关系汇总」+属性/取值富展示+悬停边见关系·属性·取值+整数id钻取修复）
- ~~**RAG 真实生成（需 LLM key·档 B·原唯独阻塞）**~~：✅ 已完成（W402·provider 化 Base URL + LLM_API_KEY 配于本地 .env 即接通真实生成·key 存在即生成/无 key 回退模板·可选 embedding 向量检索增强；W358 前端 draft 渲染 + 后端 history 多轮上下文接缝）。**平行替代入口**：启用 `xiyouji-agent-web`（Web Agent「西游记·渡口问津」·CodeBuddy Agent SDK·`CODEBUDDY_API_KEY`·PROJECT_CWD=D:/1/xiyouji·直接对话/检索 docs/ + 跑 scripts/ + 写 dataset/），凭 CodeBuddy API 即开箱"能问"，与 scripts/rag 档B 二选一或并行（详见 `xiyouji-agent-web/README.md`）
- ~~**发布与可发现性**~~：✅ 已完成（W390 GitHub Pages 上线 `https://1273984347.github.io/xiyouji/` + W399 SEO 域名补全 sitemap/robots + W394-W398 英文站 batch1-5 扩至 65 文件）
- **内容质量深化（非增量）**：A1 逐回补交叉引用/结构化元数据、A5/A6 提质、全站死链与术语统一

### A 方向：docs 内容扩容

#### A2 个人随笔扩容（A2 Batch 1-10 已完成 W066-W086）

已创作 44 篇差异化主题随笔，A2 矩阵扩展到 43 维度。Batch 1-10 系列收束。

**W073 方法论升级（毕业）**：Preflight 双轨验证（W072 教训吸收）—— Preflight 阶段验证 line 号引用时必须双轨：Grep 验归属 + Read 验内容匹配。已毕业到 [docs/10-方法论沉淀/Preflight与Subagent模板.md](docs/10-方法论沉淀/Preflight与Subagent模板.md) 第三节。

#### A3 人物分析深化（已完成 W075-W077 + W040 + W132-W160 + W179 + W237-W252 + W285）

docs/02-人物深度分析/ 已有 215 篇。可继续深化方向：

- 明代历史原型对照深化（如王阳明 vs 唐僧心学/张居正 vs 玉帝权术/海瑞 vs 悟空直谏）
- 女性人物专题扩容（铁扇公主/白骨精/玉面狐狸等深化）
- 取经五众心理深度画像补遗

#### A4 主题专题扩容（已完成 Batch 1-20，W078-W190 + W285-W286）

docs/03-主题与情节专题/ 已有 209 篇。已完成方向：

- ✅ Batch 1-9（W078-W086）：权力五联对照闭环（来源→制度化→工具化→空间化→谱系化）+ 全球比较 + 数学结构 + 组织学 + 时间哲学 + 空间政治学 + 经济学专题九个新维度
- ✅ Batch 10-12（W088-W090）：时间哲学 + 空间政治学 + 地理符号学形成"时间-空间-路径"三维结构
- ✅ Batch 13-15（W091-W093 + W097-W098）：心理学 + 妖怪经济学 + 风险管理 + 开篇诗 + 回目对联
- ✅ Batch 16（W101）：取经团队身体地理学专题（梅洛-庞蒂身体现象学 + 福柯生命政治 + 巴特勒性别表演 + 德勒兹无器官身体 + 阿甘本赤裸生命 + 列维纳斯面容伦理·与 W088+W089+W090 形成"时空身体"三元结构）

**A4 方向系列建议**：Batch 1-20 已完成 101 篇主题专题（含十六维叙事学框架 + 全新领域开拓四篇：生态批评/后人类主义/物质生态学/动物研究）。W285-W286 新增 89 篇（68 篇主题系列 .txt 拆分 + 21 篇详解.txt 随笔拆分），W359-W383 再增 8 篇（W369-W383 第二批 5 篇 + 边缘人物/理论新视野方法论），累计 209 篇。下一 batch 候选可继续新维度扩展（如取经网络叙事学专题·基于海尔斯技术生成认知 / 取经地理批评专题·基于韦斯特法尔地理批评 / 取经情感结构专题·基于雷蒙德·威廉斯情感结构理论）。

#### A5 文化与历史背景扩容（已完成 W094-W096 + W126 + W130 + W134 + W142 + W146 + W150 + W154 + W180·八层明代镜像完整闭环）

docs/04-文化与历史背景/ 已有 34 篇。可扩方向：

- 佛道思想深化（禅宗公案 vs 内丹学/三教合一明代思想史）
- 明代社会史镜像（市井百态/科举制度/海禁/卫所制）
- 版本演变史补遗（西游记古本/评点本/海外译本深化）

#### A6 诗词歌赋扩容（已完成 W097-W098 + W288 + W351/W356/W357 英文译介全覆盖）

docs/05-诗词歌赋/ 中文专题已齐备，**英文站译介（site/en/ E6/E31/E33/E34-E37）已全覆盖全部诗词专题**：人物赋(E34)/韵律分析(E35)/主题创作(E36)/原著赏析(E37)/景物诗(E6)/开篇诗(E31)/意象谱系(E33)。中文侧无新增 gap，英文侧亦无剩余未译专题。

- ~~原著诗词分类赏析（人物赋/景物诗）~~：✅ EN 译介已完成（E34 人物赋 / E6 景物诗）
- ~~个人创作补遗（仿古诗词/现代诗改写）~~：✅ EN 译介已完成（E36 主题诗词创作）

### V 方向：可视化深化（site/data/ 86 页）

- **V1 现有页面内容深化**：dashboard KPI 卡片数据更新 / 可视化交互增强（hover/click/筛选）
- **V2 新增可视化维度**：西游地理 3D 可视化（Cesium/Three.js）/ 人物关系动态网络（d3-force）/ 八十一难难度热力图深化 / 取经路线时间-空间双轴
- **V3 跨页面导航增强**：footer 关联链接补全 / 标签云 / 智能推荐
- **V4 移动端体验**：375px 视口回归 / 触摸交互优化 / 移动端专属可视化（竖版图表）

### E 方向：工程化（E1-E8 已完成）

- **E1 CI/CD 深化**：已完成 .github/workflows/ 5 文件·三轨并行 screenshots+lighthouse+a11y+security+perf
- **E2 a11y 审查**：已完成 19 项检查覆盖 20 条 WCAG 2.2 SC（a11y_audit.py·W422 口径修正：CI job 名「9-rule」、脚本 docstring「40 条」、实际 19 个 check 函数三方不一致）
- **E3 性能优化**：已完成 perf_optimize.py 6 模块 LCP/Canvas/CSS/JS/Minify
- **E4 国际化（i18n）**：已完成 site/en/ 英文站 65 文件（W394-W398 batch1-5 英文化扩站）
- **E5 测试体系**：已完成 Playwright E2E 三层测试 + 90 unit passed
- **E6 性能监控**：已完成 Core Web Vitals 五指标 + RUM 采集 + perf-budget.json
- **E7 SEO 优化**：已完成 JSON-LD 结构化数据 + sitemap + robots.txt + OG 标签
- **E8 安全加固**：已完成 security.yml 4 job + security_scan.py + site/_headers 5 安全头

### S 方向：分享发布

- **S1 项目问题与解决方案教学材料化**（✅ 已完成 W070，2026-07-26）：docs/10-方法论沉淀/ 5 篇主题 + 1 篇索引
- **S2 关键随笔/专题发布**：挑选 12 篇 A2 个人随笔 + 15 篇 A4 主题专题，发布到博客/公众号/知乎
- **S3 项目方法论沉淀外部分享**：基于 docs/10-方法论沉淀/ 12 篇主题，整理成对外可发布的方法论文章·**W299-W301 首批 3 篇 + W302-W305 第二批 4 篇 + W306-W309 第三批 4 篇 + W310-W311 第四批 2 篇（记忆研究理论框架 / 可视化测试计划）均已落地（共 13 篇内部方法论已全部发布）**·（写作规范 + 多代理并行模式 + Edit 竞态决策树 + 双索引可追溯改造 + Subagent 盲信铁律 + 改动后影响面扫描 + E2 文档同步区分历史vs现役 + DRL 真循环 + 三 skill 闭环 + E1 铁律 + Preflight 与 Subagent 模板）·S3 方向收束
- **S4 学术投稿**：基于 docs/04-文化与历史背景/ + source/引用与网络解读/学术论文索引.md 50 条，撰写学术论文投稿

### 优先级建议

- **短期（W287+）**：A4 主题专题 Batch 16+ 扩容 / A5 文化背景 Batch 2+ 扩容 / A6 诗词歌赋 Batch 2+ 扩容 / V1 可视化深化
- **中期**：V2 新增维度 + E1 截图审查 CI 化
- **长期**：E2 a11y + S2/S3/S4 分享发布

---

## 三、方法论沉淀（复利经验库）

> 每轮 W 编号完成后，将可复利沉淀的方法论追加到本段。格式：W### + 经验名 + 简述 + 复现计数器。
> 历史方法论记录详见 [交接文档-archive.md](交接文档-archive.md)。

### W528：bump_version.py 简单页脚替换盲区（页脚滞后即永久脱节）

- **经验名**：bump_version.py 对 index / cross-time-danmaku / tag-cloud 三简单页脚的同步是**替换式**——正则 `v{old_ver}·W{old_w}`（old 取自 dukou-engine footer 解析出的上一版本），只匹配"链首恰好等于当前解析版本"的条目；页脚一旦滞后（链首 ≠ 解析版本）就永不命中，从该批起逐批脱节且**静默**（bump 输出"已同步"却实际零改动）。W528 实测：三页脚停在 v2.3.68 W453，W453-W527 共 74 批从未被更新；verify_delivery 仅校验 dukou-engine footer（新鲜度 WARN），三简单页脚完全不在校验内 = WARN 级盲区。
- **处置**：W528 手动 prepend 现役条目（`v2.3.127 · W528 存量漂移点统一修复 · <原链>`，历史链 prepend 模式与 dukou-engine 一致）恢复，下一批 bump 即可正常替换链首；预防 = bump 跑后 Grep 三页脚确认版本实际前进（并入 bump 坑①②的跑后 Grep 校验清单）。
- **复现计数器**：1/1（W528 首次定位并修复；凡 bump 后必须校验三页脚实际前进，防止再脱节）

### W523：CI 中 git diff 输出做路径分类必须 -c core.quotePath=false（中文路径八进制转义坑）

- **经验名**（E47）：CI workflow 里用 `git diff --name-only` 输出做 bash case 文件分类时，默认 `core.quotePath=true` 会把非 ASCII 路径渲染成带双引号的八进制转义串（如交接文档.md → "\344\272\244..."），首尾引号破坏 glob 匹配——连 `*.md`、`docs/*` 这类宽模式都失配，全部落入兜底分支。本项目表现为：铁律 #1 要求每批同步中文文件名的交接文档.md → Screenshot Review 的 scope 免审判定恒失效 → 每次推送触发 ~11min 保守全量截图（W421 提速机制被静默架空数月，无任何报错）。
- **处置**：screenshot-review.yml scope 步骤三处 git diff 调用点统一加 `-c core.quotePath=false`（push 零 SHA 兜底 / push BEFORE..SHA / PR base...sha）；验证方法论——本地 Git Bash 复刻 yml case 块逐字逻辑，对真实历史范围双向模拟（旧命令精确复现 full 分类 / 新命令命中 skip），不依赖远端 CI 试错；规则已入 AGENTS §4.3。
- **复现计数器**：1/1（W523 首次定位并根治；凡新增用 git 输出做路径分类的脚本/workflow 必须沿用此规则）

### W520：递增数字禁字面量（skills 内文数字双防线外盲区收编）

- **经验名**（E46）：随批次演化的计数（门禁数/篇数/页数/skill 数）在持久文档的**现役状态描述**中写裸字面量，必然随项目演化漂移成误导性陈述——且 skills 内文数字游离于「六文档同步契约」与「计数门禁（verify_delivery A 类断言）」两道防线之外（check_skills_index 只管结构不管内文数字，构不成第三道防线），属结构性盲区。W519 skills 全目录审查实证存量文档门禁数多处滞后；W520 本批自体病例——day-review 验证清单写死「六项」而步骤 4 实为 7 条、drift-audit reference「修复后验证」区写死「17 门禁全绿」；B 类修复已验证治本写法——改为引用式表述（「以 verify_delivery 输出为准」「见 README 权威值/统计口径说明」）或绑定实测时点与来源（门禁依赖断言字面量「A4 209 篇」「共 N 篇」受 verify_delivery EXPECT_A4/A1-A6 保护，属豁免禁改引用式）。（编号注：E45 已由 W460-W463「W 批收尾三坑」经验占用——该经验在 project_memory 以 E45 标注、未在「三」区登记，故本条顺延 E46；补登记待办见下。）
- **处置**：写作侧根治——plan-authoring SKILL 去模糊化成文标准新增第 5 条「递增数字引用式」+ day-review SKILL 步骤 4 新增第 8 条「递增数字字面量扫描」；体检侧兜底——drift-audit SKILL 步骤 5 第 4 条固化为「skill 内部数字引用 vs 权威值比对」固定维度（reference 命令区新增排查命令）；三 skill 自体病例同批治愈。本段为交接文档「三」强制登记位。
- **复现计数器**：3/3（W499 第三轮 day-review「15 条门禁」滞后 + W519 存量多处滞后 + W520 自体病例「六项/17 门禁」——本规则落地后由 day-review 第 8 条 + drift-audit 固定维度持续拦截）

### W463：W 批收尾三坑（bump --desc 追加污染 / CHANGELOG 大段手工编辑 / 新批版本号撞号）

- **经验名**（E45）：W 批次收尾期三个高频坑——① `bump_version.py --desc` 会往 STRUCTURE.md L4 / 项目说明.md L5 的上一版本行**追加**「 + W46X(...)」而非替换主描述（W461/462/463 三批三犯，跑后必查三处并 Grep 验证）；② 大型 CHANGELOG 多版段插入/重排只用脚本 + 结构断言（锚点唯一性 + 版段 order 校验），手工 Edit 大段必吞相邻版段标题（W463 两连错）；③ 新批版本号 = Grep 现役段 max+1 再写，凭记忆必撞号（W463 撞 v2.3.77 实证）。
- **处置**：规则已入 AGENTS.md §4.3 工具链要点（bump 坑 / CHANGELOG 插段 / 版本号 pre-grep 三条）；本段为交接文档「三」强制登记位——2026-08-25 W520 批**补登记**：该经验 2026-08-17 W460-W463 复盘时已在 agent memory（project_memory / experience-log）以 E45 标注、未走登记位，本批补录使编号空间完整（W520 段 E46 顺延即因此占用）。
- **复现计数器**：3/3（W461/W462/W463 三批三犯毕业）

### W517：共享机制载体铁律（仓库内文件为真源·全局路径不共享）

- **经验名**：任何要让其他 Agent 读到的机制/规则/经验，必须写入**仓库内文件**（git tracked：AGENTS.md / 交接文档 / skills/ / docs/10-方法论沉淀/），**禁止只写全局路径**（如 `c:\Users\...\.trae-cn\skills\`）——W516 实证：上移机制曾误写全局版 mem-wrap-up，其他 Agent 在项目 session 读的是仓库版（全局修改不可见），且 `sync_skills.py --sync` 为**仓库→全局单向**（全局修改会被下次同步覆盖）。改 skills/ 下任何 skill 必须改仓库版后 `python scripts/sync_skills.py --sync` 同步全局。
- **处置**：规则已入 AGENTS §4.3；本段为交接文档「三」强制登记位。W516 修正过程见 CHANGELOG v2.3.115（commit 72a9276）。
- **复现计数器**：1/1（W516 首次，用户指出载体错误后修正；本规则防止再犯）

### W516：经验上移机制固化 + 剩余经验补上移（E44/E34/E41/E36-42 五组）

- **经验名**：上移机制已固化进 mem-wrap-up Step 5（毕业路径 + 上移映射表 + 强制登记交接文档「三」）；本批补上移 memory 中尚未进项目公共载体的高价值经验——
  - **E44 三 skill 触发门控三时刻**：W### 开始 / 修复批后 / session 收尾 三时刻强制自查 skill 触发（brainstorming/writing-plans/DRL），不等 user 质问（W401 跳 DRL 闭环漏检 F1+N1-N10 教训）——规则已入 project_memory Hard Constraints，现补登记本段
  - **E34 PowerShell heredoc 替代**：Windows 环境多行字符串一律走 Write 临时文件 + `-F` 参数 + Delete，避免 `cat <<'EOF'` heredoc 报错（2026-08-25 W514 git commit -F 中文提交信息二度复用）
  - **E41 跨 session 先确认远端**：涉及远端状态的执行前先 `git log --oneline -5` + `gh run list` 确认最新状态，识别并行交付真实增量（2026-08-08）
  - **E36-E42 workflow/CI 类 7 条**：① workflow 触发条件必须匹配真实开发流（直 push main 必须有 push 触发）；② 门禁建置即绿（不允许"从未运行/从未通过"的门禁存在）；③ workflow 引用文件先用 `git ls-files` 确认存在；④ CI 阈值基于真实测量+波动余量校准（性能门禁用预算断言而非总分）；⑤ cache 仅在 job 有依赖安装步骤时配置；⑥ gh 复杂查询走 Write 临时 .ps1 + `powershell -File` + Delete；⑦ 门禁覆盖范围必须自检（正则形态漏检坑，W424）
- **处置**：E44/E41/E36-42 均为稳定机制/复现经验，符合毕业判据；本段为交接文档「三」强制登记位；后续每批复盘收尾时对照本段自查是否已进项目载体
- **复现计数器**：E44 1/1（W401 补跑）· E34 2/3（W514 二度复用）· E41 1/1（W400 系列）· E36-42 各 1/3（W400/W412 系列）——部分规则已入 AGENTS §4.3 或 project_memory，本段作为公共登记位供所有 Agent 可见

### W505-W508：内容可信度轨经验固化（引文/术语/BOM/归档四类，从 agent memory 上移共享）

- **经验名**：AI 生成内容防"看似正确的编造"四类规则——① 引文必须经 `scripts/_cite_probe.py` 从 text-search.json 提取候选再写 `> 原文引文` 行（W505 高翠兰篇凭记忆编造 FAIL：check_citations 只拦"写入后未命中"，拦不住"恰好像原文的句子"）；② 新文档（非基线豁免）用变体称谓（行者/唐僧/圣僧）必须同时含 canonical（孙悟空/玄奘），写作前 Grep `dataset/glossary.json` 称谓组（W505 3 篇 C2 FAIL）；③ 批量改 md 禁 PowerShell `Set-Content`（写 UTF-8 BOM，W427 曾全库清 BOM）；④ 归档/删除脚本必须同步 Grep tests/ 引用（W447 归档 fix_svg_negative_widths.py 漏删配套测试致 pytest 收集失败，W506 处置）。
- **处置**：工具已永久化 `scripts/_cite_probe.py`（W507）+ `scripts/_check_pipeline_consistency.py`（W508 管线标记/生成来源/引文数轻量校验）；规则已写入 AGENTS.md §4.3 + §6 铁律 13；day-review skill 步骤 4 新增"归档删脚本查 tests/"检查项。
- **复现计数器**：引文编造 1/1（W505）· 术语 C2 3 篇（W505）· BOM 1/1（W505）· 归档漏删测试 1/1（W506）· Edit 串行 4 次（W505）——规则均已进项目级载体，后续 Agent 无需重复撞坑。

### W507-W508：复盘行动项闭环方法论（memory → 项目 上移机制）

- **经验名**：agent memory（experience-log/quickref/project_memory）是**私有**层，其他 Agent 读不到；复盘结论要防重复犯错，必须**上移**到项目公共载体——规则类进 AGENTS.md（铁律/工具链要点）、批次经验进交接文档「三、方法论沉淀」、深度篇进 docs/10-方法论沉淀/、流程进 skills/。W070 已确立此模式，W507-W508 为其续作（本次将 W505-508 四类规则全部上移）。
- **复现计数器**：1/1（W507-W508 首次系统性上移；后续每批复盘收尾时对照"是否已进项目载体"自查）

### W418：bump_version 全局替换会污染 file-index 历史段（E2 判据的机器执行坑）

- **经验名**：跑 `bump_version.py` 后必须 Grep 验证 file-index W417 及更早历史段未被全局替换污染（本次 W417 段页脚 3 行被误改为 v2.3.33 W418，违反 E2「历史条目保留旧值」）
- **处置**：恢复历史段原值 + 新改动正确登记到当前 W 段；bump 只信任它改 README/STRUCTURE/项目说明的版本行，file-index/交接文档历史段要人工核
- **复现计数器**：2/2（W418 首次撞坑 + **W419 复现**——W418 段页脚 3 行（site/index/cross-time-danmaku/tag-cloud）被误改为 v2.3.34 W419·已按 E2 恢复历史原值。结论：该坑已成固定模式，bump 每次跑完必须 Grep 校验上一 W 段页脚行）

### W419：修复脚本"先回退非必要改动"最小化 diff（E1 铁律的机器执行坑）

- **经验名**：批量重写型修复脚本（如 SD 归位重建所有回文件）会对"SD 集合未变"的回文件也执行 `pre.rstrip()` 等格式化，产生非必要 diff（1-37/73-100 回仅删多余空行）——违反"只做必要修改"
- **处置**：脚本改完先 `git diff --name-only` 对比改动范围，对"本应无变化"的文件 `git restore` 回退，仅保留真实修复目标（W419 回退 65 个文件·保留 38-72 回 35 个）
- **复现计数器**：1/1（W419 首次）

### W419：回文件段解析正则的衔接坑（markdown 结构修复）

- **经验名**：`re.search(r"\n## 深度解读\n")` 会吞掉 `## 深度解读` 前导换行，导致 `pre + new_sec` 拼接时导航行与段标题间无空行（L92 导航行后直接 `## 深度解读`）；且若上一次运行已破坏结构（行内 `## 深度解读` 无前导 \n），正则匹配不到 → 二次运行时把新段追加到文件尾造成重复块
- **处置**：解析正则不要求前导 \n（`r"## 深度解读\n"`）+ 拼接统一 `pre.rstrip() + "\n\n" + new_sec` 保证空行衔接；出现结构损坏先 git restore 全量回退再重跑，不叠加修复
- **复现计数器**：1/1（W419 首次）

### W420：链接审计不能只查"存在"要查"指向正确"（导航错链坑）

- **经验名**：lint_links 只查目标文件是否存在，不查"上一回/下一回"是否指向**相邻回**——W418 声称"100 回导航全覆盖"，但约 60 回导航指向非相邻回（如第8回下一回直跳第13回·第38回上一回指第36回），此前从未被校验
- **处置**：对 `> 导航：` 行做**相邻性断言**——提取上一回/下一回链接目标编号，断言 = 第N-1/第N+1 回（第1回无上一回·第100回保留 `[全书完]`）；W420 修复 99 回后 100/100 通过。后续可考虑把该断言纳入 verify_delivery 或 pre-commit
- **复现计数器**：1/1（W420 首次·后续链接质量类任务必查语义正确性而非仅 404）

### W420：批量重写脚本必须保留原行换行符（CRLF 混合换行坑）

- **经验名**：Python 以 `newline=''` 读写时，multiline 正则 `.*$` 会吞掉行尾 `\r`——导航行替换后 99 个 CRLF 文件各出现 1 行裸 LF，git diff 显示为"无文本差异的 M"（autocrlf 归一化掩盖），实际工作区混合换行
- **处置**：写回前按文件主导换行符修复（CRLF 主导则导航行补 `\r`）；字节级替换（`rb/wb`）最稳妥。教训：批量重写后必须 `git diff --name-only` + 抽查字节级换行，不能只看文本 diff
- **复现计数器**：1/1（W420 首次）

### W421：workflow 提速——paths 过滤挡不住"内容型"变更，需步骤级 diff 判定

- **经验名**：GitHub Actions `paths` 过滤只认路径不认内容——版本 bump 必改 site 页脚（属 `site/**`），Screenshot Review 每次都触发 13 分钟全量截图；优化必须做**提交内容分类**（页脚/文档-only 跳过·data 页定向截图·static/脚本/workflow 全量）
- **处置**：Checkout 后加 diff 分类步骤（`git diff --name-only before..sha`）+ 后续重步骤 `if` 门控 + 跳过时 GITHUB_STEP_SUMMARY 输出原因；Checkout 必须 `fetch-depth: 0`（fetch-depth 1 时 `github.event.before`/`pull_request.base.sha` 本地不存在，git diff 直接失败——W421 新坑）
- **复现计数器**：1/1（W421 首次·后续 workflow 优化类任务沿用）

### W422：门禁"从未运行"类问题二犯——触发条件必须逐 workflow 核对真实开发流

- **经验名**：W399 ci.yml 仅 pull_request 触发、项目直 push main 无 PR → CI 从未运行（W400 复盘已列雷区 #1）；W422 复现同模式：perf.yml（LHCI LCP/CLS/TBT 硬预算）仅 pull_request + workflow_dispatch → 性能门禁从未执行。**逐 workflow 核对触发矩阵**必须成为审计必查项
- **处置**：perf.yml 补 push main（site/**）+ 每周定时；verify_delivery 新增链接/导航/sitemap/回退 4 项门禁后，此类"声明≠落地"靠自动化兜底
- **复现计数器**：2/2（W399 ci.yml + W422 perf.yml）

### W088-W099：Preflight 三轨验证稳定有效 + E1 预防策略成熟

- **经验名**：Preflight 三轨验证（line 号归属 + 内容匹配 + chapter 归属 Grep num:N）+ 主代理 spot-check + E1 预防策略（创作后立即 git add）
- **简述**：W088-W099 连续 12 篇专题均通过 Preflight 三轨验证，发现并修正 12 处 line 号错误，E1 铁律 W091 撞坑后 W092-W099 连续 8 次预防成功
- **复现计数器**：12/12（W088-W099 连续 12 次复用，验证稳定有效）

### W070：S1 教学材料化方法论首次完整沉淀

- **经验名**：将散落在 user_profile.md + project_memory.md + 上游 skill 文件三处的可复利方法论集中沉淀到项目内 docs/10-方法论沉淀/，形成项目级权威源
- **复现计数器**：1/1（W070 首次完整沉淀，5 篇主题 + 1 篇索引）
- **关联文档**：[docs/10-方法论沉淀/README.md](docs/10-方法论沉淀/README.md)

### W071-W073：A2 方向 Preflight 双轨验证方法论毕业

- **经验名**：Preflight 双轨验证（Grep 验归属 + Read 验内容匹配）+ 主代理直接基于已验证事实清单创作 + 主代理 spot-check
- **毕业判据应用**：稳定机制 ✓ + 反复出现 ≥3 次 ✓ + 接手者必须知道 ✓ → 走"毕业路径"
- **复现计数器**：3/3（W071 一次通过 / W072 发现 2 P1 / W073 一次通过）

### E1 跨 session 接续 git tracked 验证铁律

- **经验名**：prior session 报告"edits made to N files"时，主代理必须用 `git ls-files <file>` 验证文件是否实际被 git tracked
- **验证命令**：`git ls-files README.md` 返回文件名 = tracked；空输出 = untracked
- **复现计数器**：21/21（最近一次：W091 prior session 创建后未 git tracked，本 session 立即 git add 修复）

### E1 升级版：修复落地验证

- **经验名**：prior session 报告"修复已落地"时，主代理必须用 Grep spot-check 验证文件内容是否实际包含修复后的值
- **验证方法**：Grep 搜索修复前的值（如命中说明修复未落地）；Grep 搜索修复后的值（如命中说明修复已落地）
- **复现计数器**：6/6（最近一次：W084 prior session summary 报告"6 文件文档同步已落地"实际 3 文件未落地）

### Subagent 工具证据不可盲信

- **经验名**：subagent 报告"已保护/未找到"项需主代理 spot-check
- **简述**：subagent 可能产生幻觉或基于 ripgrep 跳过二进制文件的结果误判。主代理在关键路径/声明上必须执行直接 Read/LS/Grep 验证

### 双索引可追溯改造

- **经验名**：CHANGELOG W### ID（正向时间线）+ file-index.md（反向文件索引）
- **使用场景**：1) 时间点→变更；2) 文件→W 条目；3) 跨文件影响面追溯

### 三 skill 闭环

- **经验名**：deep-review-loop（审查）→ mem-wrap-up（收尾）→ self-evolution（沉淀）
- **简述**：DRL 收敛 → mem-wrap-up Step 7 → self-evolution dim 1/5/9/11。如发现跳过某 skill，视为闭环断裂，立即补跑
- **位置**（仓库版，2026-08-19 治理后以仓库内副本为准）：
  - `skills/agent-session-loop/SKILL.md`（三阶段整合流水线：审查→收尾→沉淀）
  - `skills/deep-review-loop/SKILL.md`（审查端，起点）
  - `skills/mem-wrap-up/SKILL.md`（收尾端，中段）
  - `skills/self-evolution/SKILL.md`（沉淀端，末端，反向喂回 DRL）

### Subagent prompt 模板铁律

- **Preflight interface analysis**：派 subagent 执行 Task 前，若 Task 引用外部 API/函数/模块/config key，主代理必须 Grep/Read 实际函数签名、import 路径、参数名、config key 名
- **Scope-lock constraint**：subagent prompt 必含硬性约束："Only modify files listed in the Files section. Do NOT modify any other files, even if you notice bugs or improvements in surrounding code."
- **Preflight fact verification**：Task 模板包含事实声明时，主代理必须独立 Grep 验证关键事实

### 改动后主动影响面扫描

- **经验名**：任何改动完成后，必须主动执行"影响面扫描"——Grep 搜索所有可能引用该改动内容的文件，全部同步更新
- **扫描范围**（最小集）：docs/00-导读/ + README + STRUCTURE + CHANGELOG + memory 文件

### 并行 Edit 竞态问题

- **经验名**：同一文件的多个 Edit 并行执行时，后执行的 Edit 可能基于原始内容覆盖先执行的 Edit 结果
- **处置**：同一文件的多个 Edit 应串行执行；若多行有共同模式，用 replace_all=true 一次性处理

### W424：门禁覆盖范围必须自检（正则形态漏检坑）

- **经验名**：`check_js_syntax.py` 原用 `<script>` 精确匹配，带属性脚本全部漏检——门禁全绿但 EN 两页坏脚本线上全坏；CSP 生成器 `META_RE` 也曾匹配不到自己写入的 meta（content 内全是单引号）
- **处置**：① 门禁正则覆盖全部合法形态（无 src 脚本无论是否带属性）② 新门禁/生成器先做**负样本自测**（构造坏文件确认能抓到）+ 注入→校验→幂等二次注入往返自测 ③ 全站 Chromium 实测与静态门禁交叉验证
- **复现计数器**：1/1（W424 首次·后续新增门禁必跑负样本）

### W424：浏览器安全机制口径以实测为准（CSP 哈希坑）

- **经验名**：按"去首尾空白"推演 CSP 内联脚本哈希 → 12/12 页全挂；Chromium 对照实验证明浏览器按**解析后原始文本**哈希（含首尾空白·不解码实体）
- **处置**：CSP/SRI/哈希类口径一律真实浏览器对照验证（SRI 一次过即因先做了 CDN 字节核对）；口径与实验结论写进生成器 docstring 防再推演
- **复现计数器**：1/1（W424 首次）

### W424：机械腐蚀是"面"不是"点"（EN 腐蚀第二波）

- **经验名**：W424 早前只修 journey-geo-semiotics 一页，实测又发现同源腐蚀 character-relationship-3d（""X"" 32 处）与 character-appearance（模板反引号 4 处）——报告修单页 ≠ 模式全清
- **处置**：发现模式化机械损坏后按**模式全站扫描**（check_corruption.py R1 + check_js_syntax 语法门禁），不按报告逐页修
- **复现计数器**：1/1（W424 首次）

### W424：静默降级会掩盖真实缺陷（d3-sankey 漏引）

- **经验名**：6 页使用 d3.sankey 但漏引插件，`typeof d3.sankey !== "function"` 仅 console.warn 后跳过——桑基图从未渲染却无人发现
- **处置**：check_corruption.py R2 插件引用门禁；后续降级路径须留页面可见标记
- **复现计数器**：1/1（W424 首次·6 页同坑）

### W424：全站批量改动必须全站实测

- **经验名**：12 页代表性抽样全过，全站 159 页扫描才暴露 EN 双页语法损坏 + sankey 空图
- **处置**：批量机械改动（CSP/SRI/字体/内嵌数据）提交前跑本地全量 Playwright（scripts/_csp_check.js 模式·约 10 分钟）+ CI Screenshot Review 双保险
- **复现计数器**：1/1（W424 首次）

### W424：CI 失败先分类（基建抖动 vs 代码）

- **经验名**：runner 起 http.server 3 秒超时首跑误报失败，rerun 即绿——与代码无关
- **处置**：失败先看日志归因；基建抖动直接 rerun；ci.yml server 启动已加重试防复发
- **复现计数器**：1/1（W424 首次）

### W424：报告引用完整性

- **经验名**：对抗性整合报告"整合来源"引用的 3 份子报告从未作为独立文件留存——读者按名查找会落空
- **处置**：报告发布前校验引用的文件存在；历史底稿已并入的报告在原文注明"未单独留存"（本批次已修）
- **复现计数器**：1/1（W424 首次）

### W459：JS 拼接链接是静态链接门禁的盲区（方案约定≠文件系统现实）

- **经验名**：lint_links 只扫 HTML 静态 href/src 与 Markdown 链接；JS 运行时拼接的 URL（如 journey-spacetime D2 按方案文档错误约定拼 `第NNN回-回目摘要.md`）不在覆盖内——方案文档写错命名约定 → 实现照抄 → 100 条跳转全 404 → lint_links 0 broken 照常放行。另注意相对路径深度：site/data/ 页到仓库根 docs/ 须 `../../`（Pages 只部署 site/，docs 直链本就是本地模式特性，与 dashboard 既有口径一致）
- **处置**：新增 `scripts/check_dynamic_links.py`（提取内联 script 字符串字面量链接做存在性校验·带 --self-test 负样本自测·已挂 verify_delivery）；写 JS 链接优先内嵌映射表（从目录实际文件名生成）代替字符串拼接文件名；新门禁首跑即抓到 2 处存量死链（tag-cloud dashboard 条目/EN source_doc 虚构路径）证明该盲区确有存量负债
- **复现计数器**：1/1（W459 首次）
---

## 四、待办事项（短期）

- [x] **治理文档健康指标超标归档（W511 已处置）**：文档规范 §8 规定「CHANGELOG/file-index >50KB 或 500 行→归档」「交接文档里程碑概要 >100 行→保留最近 5 版」，实测 CHANGELOG 643 行/158KB · file-index 772 行/82KB · 交接文档里程碑概要 408 行 全部超标——W511 已归档：CHANGELOG W417-W464 段迁 archive（→49KB/235 行）·file-index W417-W448+损坏区迁 archive（→35.8KB/335 行）·交接文档概要保留最近 5 版（→19 行，W505 及更早归档至 交接文档-archive.md）·三文档体积/行数实测达标
- [x] **LHCI 性能债专项（W423 已处置）**：W422 push 首跑暴露的 index LCP ~4.7s（dashboard head 同步 D3 真凶）/timeline CLS ~0.24（CJK 字体 swap 回流）已优化——CJK 字体 swap→optional + D3/Three 移出 head + perf.yml 预算收紧（LCP 5000→4500·CLS 0.3→0.2）；本地无浏览器，LHCI 收紧后实测以 CI 为准
- [x] A2 Batch 1-10 个人随笔扩容（已完成 W066-W086，44 篇）
- [x] S1 项目问题与解决方案教学材料化（已完成 W070，docs/10-方法论沉淀/ 5 篇主题 + 1 篇索引）
- [x] A3 Batch 1-4 人物深化扩容（已完成 W075-W077 + W040，23 篇）
- [x] A4 主题专题扩容 Batch 1-15（已完成 W078-W098，28 篇）
- [x] A5 文化与历史背景扩容 Batch 1（已完成 W094-W096，3 篇）
- [x] A6 诗词歌赋扩容 Batch 1（已完成 W097-W098，2 篇）
- [x] D 可视化深化 Batch 1（已完成 W099，1 篇·地理符号学力导向图）
- [x] v0.9.1 runtime 验证回归（cross-time-danmaku + dashboard 浏览器复测·2026-07-29 子代理浏览器 spot-check PASS·控制台无 ERROR·网络无 404/500·三个 signature moments 渲染正常·Name That UI 五组件生效·搜索框筛选"叙事学"触发空状态）
- [x] 将增强版截图审查纳入迭代发布流程（已完成 W406·screenshot-review.yml 补 push main 触发 + batch_screenshots.js 良性过滤 file:// 回退噪声→--fail-on-issues 不再误判全红）
- [x] S3 项目方法论沉淀外部分享 首批（已完成 W299-W301，docs/S3-方法论外部分享/ 3 篇 + README.md 索引）
- [x] A4 主题专题 Batch 16+ 扩容（已完成 W101-W190，十六维叙事学框架 + 全新领域开拓四篇）
- [x] A5 文化背景 Batch 2+ 扩容（已完成 W126/W130/W134/W142/W146/W150/W154/W180·八层明代镜像完整闭环）
- [x] W285 人物/主题系列 .txt 源文件整理迁移（14 个根目录 .txt 精细拆分·A3 76→197 篇 + A4 101→169 篇）
- [x] W286 source\原文 整理为.md + 补充原文内容（详解.txt 21 篇 + 5 个深读拆分 100 SD 切片 + 100 回原文 + 合并到逐回解读.md）
- [x] W286b 格式修复（分回/*.txt→.md + temp_shendu/→shendu/*.txt→.md）
- [x] W286c 审查修复（清除第069/099回网页垃圾 + 第099回追加缺失深度解读SD075 + 7个源.txt删除）
- [x] W287 pre-commit 自动化校验门禁建立 + 文件计数声明修复（scripts/pre-commit-validate.py + .git/hooks/pre-commit 钩子 + E3 双重门禁 + A3 197→196/A4 190→189 声明修正）
- [x] A6 诗词歌赋 Batch 2 扩容（已完成 W288，3 篇·水调歌头词牌赏析/诗词意象谱系/韵律分析）
- [x] W358（v2.3.9）git commit + push（静态优先健壮性加固全部改动已提交，W359-W400 均已跟进 commit/push）
- [x] RAG 真实生成（档 B·W402 已解除阻塞）：provider 化 Base URL + LLM_API_KEY 配于本地 .env（gitignored）即接通 `answer()` 真实 LLM 生成（stdlib urllib 打 OpenAI 兼容/Anthropic 原生双适配器·零依赖）+ 可选 embedding 向量检索增强（W402 落地：key 存在即生成·无 key 回退模板·rag-chat.js 渲染 llm_generated）
  - **平行替代入口（推荐先试）**：`xiyouji-agent-web`（Web Agent「西游记·渡口问津」·CodeBuddy Agent SDK·凭证 `CODEBUDDY_API_KEY`·`PROJECT_CWD=D:/1/xiyouji` 直接对话/检索 docs/ + 跑 scripts/ + 写 dataset/）凭 CodeBuddy API 即开箱"能问"，与 scripts/rag 档B 二选一或并行（无需本仓库 LLM_API_KEY）；详见 `xiyouji-agent-web/README.md`

- [x] 英文站续译：site/data 86 张可视化页英文化全部完成（W445 relationships 收官）+ 52 个旧 EN 页 CJK 清理（W446·138 EN 页全过 validate_en.py）
- [ ] 真实读者量验证：注册 GoatCounter（CSP 已 W424 meta 落地·Netlify 仅剩 HTTP 头增强选项），跑 inject_goatcounter.py --site xxx 切换真实跨访客统计
- [x] 安全审计剩余项（SECURITY-AUDIT-2026-08-09·W412 已处置）：P0-2 禁运行时覆盖 BASE_URL/API_KEY（前端表单移除·仅服务端 .env）·P1-2 静态站 XSS 转义 6 页（innerHTML sink）+ **site/_headers script-src 补 https://d3js.org 白名单**·P1-4 版本叙述统一（systemPrompt + 页脚）·P2 各项（rag_server 参数钳制/LLM 端点校验/SSE 超时与断开清理/依赖锁/ChatMarkdown DOMPurify 消毒·P2-4 MCP 外链核验已缓解）·P3 各项（日志收敛/CORS 白名单/死代码/版本漂移）·**SECURITY-AUDIT-2026-08-09.md 已加密归档**（7z AES-256·-mhe=on·归档 SECURITY-AUDIT-2026-08-09.7z·明文已删·密码存本地 SECURITY-AUDIT-2026-08-09.password·gitignore 不入库）·**P1-3 密钥轮换已落地**（旧 key sk-e8228… 已吊销·新 key sk-ba531… 已写入 .env·HTTP 端到端验证 LLM 生成正常）·**CSP 已落地（W424·159 页 meta 哈希·script-src 无 unsafe-inline；style-src 保留 unsafe-inline 为工程取舍）·_headers 已同步白名单（仅 Netlify/CF 平台层生效）**

---

## 五、关键文件索引

### 项目根目录

- [README.md](README.md)：项目说明（版本行由 bump 维护·以文件头部为准）
- [STRUCTURE.md](STRUCTURE.md)：目录结构详细说明
- [CHANGELOG.md](CHANGELOG.md)：更新日志（正向时间线，W001-W535；v2.2.50+ 现役段）
- [CHANGELOG-ARCHIVE.md](CHANGELOG-ARCHIVE.md)：历史版本归档（v0.1-v2.0.60，W001-W087）
- [交接文档.md](交接文档.md)：本文件（跨 session 交接，v2.2.50+ 现役段）
- [交接文档-archive.md](交接文档-archive.md)：交接文档历史归档（v2.0.39-v2.2.49，W066-W280）

### docs/ 目录

- [docs/00-导读/项目说明.md](docs/00-导读/项目说明.md)：项目状态与待办优先级
- [docs/00-导读/文档规范.md](docs/00-导读/文档规范.md)：文档写入规则（防膨胀·6 文档同步正确方式·归档触发条件）
- [docs/01-全书逐回解读/](docs/01-全书逐回解读/)：100 回逐回解读（A1 方向 100/100 收束）
- [docs/02-人物深度分析/](docs/02-人物深度分析/)：215 篇人物分析
- [docs/03-主题与情节专题/](docs/03-主题与情节专题/)：209 篇主题专题
- [docs/04-文化与历史背景/](docs/04-文化与历史背景/)：34 篇文化与历史背景
- [docs/05-诗词歌赋/](docs/05-诗词歌赋/)：诗词歌赋专题
- [docs/06-个人随笔/](docs/06-个人随笔/)：44 篇独立随笔 + 现代视角解读合集（A2 方向 Batch 1-10）
- [docs/10-方法论沉淀/](docs/10-方法论沉淀/)：5 篇主题 + 1 篇索引（S1 方向 W070 新增，W073 Preflight 双轨验证升级）

### site/data/ 可视化

- [site/data/text-search.html](site/data/text-search.html)：原著全文检索（708441 字，100 回）
- [site/dashboard.html](site/dashboard.html)：数据仪表盘（50+ 个 KPI 卡片 + 全站搜索浮层）
- 86 个可视化/交互页（含 graph-explorer·A-AH 34 类 + Q+ 双联 + Q++ 博物馆 + Q+++ 新功能 + 全文检索 + 妖怪后台论 + MBTI 演变图 + 难度热力图 + 地理符号学力导向图 + 引语情感分析 + 人物共现网络 + 关系演化时间线 + 六感/十二维/十六维/十七维叙事学关系图谱 + 四大天王器物符号图谱 + 观音六重身份关系图 + 妖怪受害者关系图 + 地府权力关系图 + 天庭权力关系图 + 互文性关系图谱 + 妖怪女性群体关系图 + W233 V3 四页 + W236 V4 Three.js 3D 升级 + W270/W274/W278/W282 跨方向整合项目可视化）

### scripts/ 工具

- [scripts/output/file-index.md](scripts/output/file-index.md)：反向文件索引（与 CHANGELOG 双向链接，W088+ 现役段）
- [scripts/output/file-index-archive.md](scripts/output/file-index-archive.md)：反向索引历史归档（W031-W087）
- [scripts/README.md](scripts/README.md)：工具目录说明（常驻工具清单 + archive 归档说明 + 约定）
- [scripts/extract_strings.py](scripts/extract_strings.py)：穷举 viz 页 chrome 文本节点 + script CJK 字面量（英文站翻译前置·W447 转正）
- [scripts/validate_en.py](scripts/validate_en.py)：校验 EN 页 chrome CJK 白名单 + script CJK=0（英文站翻译门禁·W447 转正）
- [scripts/verify_delivery.py](scripts/verify_delivery.py)：交付校验总闸
- [scripts/generate_csp.py](scripts/generate_csp.py)：全站 CSP 注入 + `--check` 零漂移
- [scripts/lint_links.py](scripts/lint_links.py)：全站死链巡检
- [scripts/archive/](scripts/archive/)：历史一次性脚本归档（45 个，W447 治理，保留 git 历史备查）
- [scripts/batch_screenshots.js](scripts/batch_screenshots.js)：Playwright 批量截图 + 布局断言
- [scripts/slice_screenshots.py](scripts/slice_screenshots.py)：Pillow 800px 切片
- [scripts/detect_unwrapped_tables.py](scripts/detect_unwrapped_tables.py)：静态表格溢出扫描

### skills/ 项目技能（playbook）

- [skills/xiyouji-version-bump](skills/xiyouji-version-bump/SKILL.md)：版本 bump 与六文档同步标准流程
- [skills/xiyouji-en-translation](skills/xiyouji-en-translation/SKILL.md)：英文站英文化流程（单页 + 并行 subagent 拆页·W447 新增）
- [skills/xiyouji-s4-submission](skills/xiyouji-s4-submission/SKILL.md)：S4 学术投稿前准备流程
- [skills/xiyouji-character-content](skills/xiyouji-character-content/SKILL.md)：人物深度分析内容创建/扩写
- [skills/xiyouji-characters-knowledge](skills/xiyouji-characters-knowledge/SKILL.md)：人物问答知识库
- [skills/xiyouji-sun-wukong](skills/xiyouji-sun-wukong/SKILL.md) / [tangseng](skills/xiyouji-tangseng/SKILL.md) / [zhu-bajie](skills/xiyouji-zhu-bajie/SKILL.md) / [sha-seng](skills/xiyouji-sha-seng/SKILL.md) / [bai-longma](skills/xiyouji-bai-longma/SKILL.md)：五个单人角色 skill

### source/ 原著与引用

- [source/原文/分回/](source/原文/分回/)：100 回原文 .md（W286 从古诗文网批量抓取·W286b 转为 .md·W286c 清除第069/099回网页垃圾）
- [source/原文/shendu/](source/原文/shendu/)：100 篇深读切片 .md（W286 从 5 个深读 .txt 拆分·W286b 转为 .md·含元数据注释标注推测回号映射）
- [source/引用与网络解读/学术论文索引.md](source/引用与网络解读/学术论文索引.md)：50 条学术论文引用
- [source/引用与网络解读/网络解读精选.md](source/引用与网络解读/网络解读精选.md)：15 条网络解读

---
## 七、环境复现（假设接手者电脑为全新状态）

### 系统要求

| 依赖 | 最低版本 | 用途 |
|---|---|---|
| Python | 3.8+ | 文本分析脚本（jieba 分词 + Pillow 图像切片） |
| Node.js | 18+ | Playwright 1.45+ 截图审查（硬性要求，Node 16 会失败） |
| Git | 2.20+ | 版本控制 |
| 浏览器 | Chrome/Edge/Firefox 任意现代浏览器 | 打开 `site/` 静态站点（支持 `file://` 协议） |

### 一键复现步骤

```bash
# 1. 克隆仓库
git clone <repo-url> xiyouji
cd xiyouji

# 2. 安装 Python 依赖（jieba + Pillow）
cd scripts
pip install -r requirements.txt

# 3. 安装 Node 依赖（playwright + 自动下载 chromium）
npm install
# postinstall 钩子自动执行：npx playwright install chromium

# 4. 验证：直接双击 site/data/ 下任一 HTML 应能在浏览器渲染 D3.js 图表
#    示例：site/data/81-hardships.html

# 5. 验证截图审查工具链
npm run screenshots        # 批量截图 + 布局断言 + 自动切片
python check_js_syntax.py --all  # 批量内联 JS 语法检查（site/data + dashboard）
python detect_unwrapped_tables.py  # 表格溢出扫描
```

### 常用工具命令（见 [scripts/README.md](scripts/README.md) 完整清单）

| 命令 | 用途 |
|---|---|
| `npm run screenshots` | Playwright 批量全页截图 + 布局断言 + 800px 切片 |
| `python check_js_syntax.py --all` | 批量检查 `site/data/*.html` 内联 JS 语法（Node `--check`） |
| `python check_js_syntax.py <file>` | 检查指定 HTML 内联 JS 语法 |
| `python detect_unwrapped_tables.py` | 静态扫描无横向滚动容器的 `<table>` |
| `python fix_svg_negative_widths.py --dry-run` | 预览 SVG 负宽度修复（不写入） |
| `python slice_screenshots.py --output-dir output/screenshots` | 全页截图切片（默认 800px） |
| `node debug_page_error.js <file>` | 调试单个页面运行时错误 |
| `node debug_console.js` | 调试单个页面控制台错误 |

### 可选本地服务（静态站点无需即可运行）

本项目主体为纯静态站点，**所有可视化页面支持 `file://` 协议直接打开，所有数据内嵌于 HTML（`EMBEDDED_DATA` 模式）**；唯一"外部"依赖是 D3.js v7 CDN（首次联网加载后浏览器缓存可用）。以下两项为**可选**本地服务（开发 / 在线浏览 / 数据 API 用，非运行必需）：
- **数据 API 服务**（W337/W339）：`scripts/api/api_server.py`（默认 127.0.0.1:8787）·提供 `/datasets` + `/graph` + `/graph/<name>` 等端点；`site/data/graph-explorer.html` 在线走 `/graph`，离线回退内嵌 `graph-fallback.js`
- **本地 RAG 后端**（W330）：`scripts/rag/rag_server.py`（默认 127.0.0.1:8777）·`site/dukou-engine.html`「渡口问津」调用 `/query`；服务未启动自动回退模板引擎

---

## 八、已知雷区（接手必读）

### 1. D3.js `file://` 协议限制（最高优先级）

- **铁律**：所有 `site/data/*.html` 必须支持 `file://` 协议直接打开
- **禁止**：`fetch()` 跨域 JSON 文件（`file://` 协议下 fetch 会失败）
- **必须**：使用内嵌回退数据模式——`EMBEDDED_DATA` 对象 / `EMBEDDED` 对象 / `FALLBACK` / `MOCK_DATA` / inline `const data`（历史页面存在多种命名，W422 起 verify_delivery 按模式集合自动校验）
  ```js
  // 模式：
  async function fetchJson(path, fallbackKey) {
    try {
      const r = await fetch(path);
      return await r.json();
    } catch (e) {
      console.warn('[INFO] 使用嵌入数据：', e.message);
      return EMBEDDED_DATA[fallbackKey];
    }
  }
  ```
- **验证方法**：`scripts/verify_delivery.py` 自动校验每个 `site/data/*.html` 命中内嵌回退模式（W422 新增·此前 Grep `EMBEDDED_DATA` 单一名会误报 42 页）
- **修改数据时**：必须同步更新两处——`EMBEDDED_DATA` 内嵌对象 + `scripts/output/data/*.json`（http server 模式用）

### 2. Playwright + Node 18 硬性要求

- `batch_screenshots.js` 依赖 Playwright 1.45+
- Playwright 1.45+ **要求 Node 18+**
- **症状**：Node 16 下 `npm install` 成功但 `npm run screenshots` 报 "Cannot find module 'playwright'" 或 chromium 启动失败
- **验证**：`node --version` 必须 ≥ 18
- **postinstall 钩子**：`npm install` 后自动执行 `npx playwright install chromium`，需联网下载约 120MB

### 3. `EMBEDDED_DATA fallback` 日志级别（勿误修复）

- `console.warn('[INFO] 使用嵌入数据')` 是**设计预期**，非警告
- **不要"修复"为 `console.error`**——fetch 失败走 fallback 是设计行为
- 截图审查时这条 warn 会被计入 console errors 但**不影响功能**
- 历史审计报告（`scripts/output/audit-baseline.md`）已记录此约定

### 4. 截图切片目录清理约定

- `slice_screenshots.py` **每次运行前自动清理** `slices/<viewport>/` 下旧 `.png`
- 手动调用切片子集时需注意：旧切片会污染当前审查结果
- 已在 `.gitignore` 中忽略 `scripts/output/screenshots/slices/`

### 5. 双索引可追溯铁律

- 每个新建/修改文件必须登记到 [file-index.md](scripts/output/file-index.md)（反向索引）
- 每个 W### 必须在 [CHANGELOG.md](CHANGELOG.md) 登记（正向索引，附四件套字段：来源/文件/验证/状态）
- **不可只更新一个索引**——两个索引必须同步
- 新增 W 条目时同时更新两个索引

### 6. A4 主题专题"七段式"模板

- 所有 A4 主题专题必须采用七段式：范围界定 / 叙事结构 / 主题深度 / 文化隐喻 / 影视改编 / 延伸思考
- **不可省略任何一段**
- 验证方法：Grep `^## ` 应有 6-7 个二级标题

### 7. D3.js v7 API 约定

- `.style(name, value)` 中 value 技术上可为函数，但**项目约定要求用 `.each()` 模式**处理条件值
- `.text()` 用于用户生成内容（XSS 防护）
- `.html()` 仅用于开发者可信的 `EMBEDDED_DATA`
- D3 v7 force simulation 必须显式 `.stop()` 或在 `tick` 中设上限，避免无限渲染

### 8. CSS 变量与设计 token

- 项目使用 27 个 CSS 变量（design tokens），定义在各页面 `:root`
- Hero 渐变固定为：`linear-gradient(135deg, #1a1410 0%, #3a2820 50%, #5a3828 100%)` + `::after` radial-gradient
- 古典宣纸风配色：背景 `#faf7f2`，主色 `#c8463a`，辅助 `#3a6b8c`
- **修改配色必须更新所有页面**，不可只改一处

### 9. 响应式断点约定

- `@media (max-width: 768px)`：平板/移动端
- `@media (max-width: 520px)`：窄屏移动端 grid fallback
- 所有可视化页面必须通过这两个断点的视觉验证

### 10. 键盘可访问性（a11y）

- `div`-based 交互元素必须配 `role="button" tabindex="0"` + 键盘事件处理器
- 验证方法：Tab 键应能聚焦所有可点击元素，Enter/Space 应能触发

### 11. 空数据保护

- 所有渲染函数必须以 `if (!data || data.length === 0) { container.append("div").text("暂无数据"); return; }` 开头
- 防止 D3.js 对空数组报错导致整页崩溃

### 12. 跨 session E1 铁律（memory 层假收敛）

- 历史 29 次复现：memory 三件套（work-log.md / topics.md / retrospective.md）W### 段缺失
- **每次 W### 落地后必须 Grep spot-check 验证** memory 三件套是否包含对应段
- 详见 [user_profile.md](file:///c:/Users/12739/.trae-cn/memory/user_profile.md) E1 升级版铁律段

### 13. GitHub Workflow 雷区（2026-08-08 W400 复盘新增）

**背景**：W399 前 CI 仅 `pull_request` 触发但项目直 push main → CI 从未运行过，W400 首次运行暴露存量问题（ruff 424 / XSS high 6 / Lighthouse 阈值 / a11y pip cache）全部修复转绿。以下为防复发清单：

1. **触发条件必须匹配真实开发流**：直 push main 的项目，workflow 必须有 `push: branches: [main]` 触发；新建 workflow 后立即 push 一次验证转绿
2. **不允许"从未运行/从未通过"的门禁**：门禁建置时必须本地先跑通（black 门禁 123/128 文件从未通过即反面案例，已移除）
3. **workflow 引用文件必须先 `git ls-files <file>` 验证存在**（`tools/screenshot-baseline.js` 从未存在导致步骤假失败）
4. **CI 阈值基于真实测量校准**：Lighthouse Performance 0.85→0.70→warn（实测 0.550-0.730）；性能门禁用 perf.yml LHCI 预算断言（LCP/CLS/TBT）
5. **cache 仅在 job 实际装依赖时配置**：a11y-audit 无依赖安装却配 `cache: pip` → Post 步骤缓存目录不存在报错使 job 失败
6. **跨 session 先确认远端**：`git log --oneline -5` + `gh run list` 防并行 session 竞态（W390-W398 并行交付案例）
7. **改 workflow 后同步 `.github/workflows/README.md`**（本次重写为 v2.3.18 现状，含触发矩阵/阈值/本地复现命令）
8. **文档同步后跑 `python scripts/verify_delivery.py`**（pre-commit hook 真实调用入口，含 A4 四文档"209 篇"校验），不是 pre-commit-validate.py
9. **gh CLI 复杂查询（--json/--jq/管道）在 PowerShell 下走临时 .ps1 文件**（参数解析不兼容）
10. **安全扫描跳过 `_` 前缀一次性开发脚本**（security_scan.py 与 ruff extend-exclude 已统一）

---

## 九、使用说明

### 跨 session 接续流程

1. **新 session 启动时**：先读本文件 + [README.md](README.md) + [docs/00-导读/项目说明.md](docs/00-导读/项目说明.md)，了解当前进度与待办
2. **接续 W 编号**：以本文件「一、当前进度」段标注的现役 W 为准；新批编号按 [CHANGELOG.md](CHANGELOG.md) 顶部「W### 编号规则」取现役段 max+1。**本文件禁止内嵌具体 W 号**（W520「递增数字禁字面量」铁律——写死的批次号必然随项目演化漂移；本行即该规则的存量病例，W534 修复）
3. **文件管控**：先读 [docs/00-导读/文档规范.md](docs/00-导读/文档规范.md) §11 文件管控清单——11.1 必须同步的文件（核心 2 份硬门禁 + 辅助 4 份 + 里程碑旁文档）/ 11.2 禁止擅自修改的文件（历史段/密钥/门禁/归档/字体源）·跨 session 多 Agent 协作强制遵守
4. **选择方向**：本文件"二、下一步方向候选清单"按优先级列出所有可推进方向
5. **方法论参考**：本文件"三、方法论沉淀"列出所有可复利经验，新 session 应遵循
6. **完成后更新**：每轮 W### 完成后，更新本文件"一、当前进度"段 + "三、方法论沉淀"段（如有新经验）+ "四、待办事项"段

### 与其他文档关系

- **本文件**（交接文档.md）：跨 session 交接，记录进度/方向/方法论/待办（v2.2.50+ 现役段）
- [交接文档-archive.md](交接文档-archive.md)：本文件历史归档（v2.0.39-v2.2.49，W066-W280）
- [CHANGELOG.md](CHANGELOG.md)：变更日志，每个 W### 附四件套字段（来源/文件/验证/状态）
- [STRUCTURE.md](STRUCTURE.md)：目录结构详细说明
- [README.md](README.md)：项目对外说明
- [docs/00-导读/项目说明.md](docs/00-导读/项目说明.md)：项目状态与待办优先级
- [docs/00-导读/文档规范.md](docs/00-导读/文档规范.md)：文档写入规则（防膨胀·6 文档同步正确方式·归档触发条件）
- [scripts/output/file-index.md](scripts/output/file-index.md)：反向文件索引（W088+ 现役段）

---

> 本文件创建于 2026-07-25（v2.0.41 W068 后），用于跨 session 交接。每次完成新 W### 后更新。最后更新：2026-08-31（v2.3.134 W535 决策闸门取数自动化：fetch_gate_stats.py 新建——GoatCounter API v0 拉近 7/30 日 UV·自测 14/14·遗留=令牌写入 .env 后一键判定）；2026-08-30（v2.3.133 W534 治理文档递增数字字面量修复：交接文档「接续 W 编号」改引用式 + 六份治理文档复扫 0 残留）；2026-08-29（v2.3.132 W533 skills 归属策略显式化 + 坑④入 playbook：四通用技能归属定案全局 master + 仓库镜像，MIRROR_SKILLS 强制；version-bump v1.2.0 收录坑④）；2026-08-29（v2.3.131 W532 交接文档最后更新滚动链裁剪回契约上限：9 批→3 批回契约上限，删 6 批逐批断言 CHANGELOG 有段）；2026-08-29（v2.3.130 W531 skills 部署全查 + 三真源降级保护：sync_skills 方向判定 + 降级拦截 + --take-global 回写 + LF 归一化·4 通用技能收敛真源）；2026-08-26（v2.3.129 W530 决策闸门工程落地：judge_gate.py 新建·判定优先级定案·读者数据复盘模板——UV 数据待用户后台回填后执行判定）；2026-08-26（v2.3.128 W529 R6 拦截落地 + W464 方案 v7 回填：goatcounter 污染实测修正（原假设不成立）·拦截按纵深防御落地 5 点·方案 12 处表述具体化）；2026-08-26（v2.3.127 W528 存量漂移点统一修复：README/STRUCTURE/交接文档 9 处静态描述对齐现役口径——skills 17→19·A3 211→215·学术索引 50→55·site/data 46→86·D3 本地化·README 索引去版本号）；2026-08-26（v2.3.126 W527 drift-audit 技能补漏维度：SKILL.md v1.2.0 步骤 3 段缺失双向核对 + 豁免区外新增乱序 P1 判级——W525 实证对齐·reference 命令/案例同步·双轨 0 漂移）；2026-08-26（v2.3.125 W526 索引健康门禁盲区封堵：check_index_health 段倒序断言 + 段缺失检测·verify 第 23 门禁旁文档同步）；2026-08-26（v2.3.124 W525 漂移审查修复：drift-audit 全仓体检处置——file-index W504 段缺失补建 + W522-W524 尾部追加重排归位·workflows README 同步至 W524·文档规范 §8 门禁表对齐 AGENTS §4.2 补学术轨/回退两项）；2026-08-25（v2.3.123 W524 bump 追加污染坑位补记：W523 批收尾实测 bump_version.py --note 同样向 STRUCTURE 头部/项目说明上一版本行追加残留——AGENTS §4.3 坑②扩为 --desc/--note 双触发 + 补记 file-index 空壳段现象·用户指令驱动再发版）；2026-08-25（v2.3.122 W523 截图审查恒全量根因修复：screenshot-review.yml scope 三处 git diff 统一加 -c core.quotePath=false——中文路径八进制转义致 case 免审失配恒落保守全量 ~11min·本地 Git Bash 对 W522 实际范围双向模拟验证 skip·规则入 AGENTS §4.3 + E47 登记交接文档「三」）；2026-08-25（v2.3.121 W522 CI 红灯修复：E41 发现本地 main ahead 25 未推 + 远端最后 CI（W503）红——scripts/ ruff 16 错误清零·行为零变更）；2026-08-25（v2.3.120 W521 存量裸字面量清剿 + W463 三坑补登记：DRL F2 15 文件三分类裁决——现状声明改引用式 6 处 + 口径/指引加注 5 处 + 门禁模板豁免；交接文档「三」补 W463 段 E45 编号空间完整化·sync 10 文件双轨一致）；2026-08-25（v2.3.119 W520 递增数字禁字面量 + skills 内文数字比对维度：plan-authoring 去模糊化标准⑤ + day-review 步骤 4 第 8 条 + drift-audit 步骤 5 第 4 条固化 + reference 命令/案例扩展 + 三 skill 自体病例治愈 + DRL 复验修复 8 项·E46 登记交接文档「三」）；2026-08-25（v2.3.118 W519 Skills 全目录审查与 SKILL.md 内容优化：A 类计数漂移 14 处（211→215 ×4·共 611→共 615 ×10·交付前复扫二轮堵漏 6 处）+ B 类门禁数去硬编码 8 处 + AGENTS 树注释 17→19 + D 类 7 skill 补 frontmatter version·sync_skills 双轨一致）；2026-08-25（v2.3.117 W518 期望版本动态化 + 尾页脚新鲜度门禁：verify_delivery 六文档期望版本动态取自 CHANGELOG 现役段·dukou-engine 页脚降级为新鲜度 WARN + check_governance_docs 检查 7「最后更新」头尾两处链首现役校验（finditer 全量扫描）+ day-review SKILL.md 步骤 4 第 7 条）；2026-08-25（v2.3.114 W515 渲染抽查常驻化 + 门禁正文引用存在性检查：render_check.js 常驻 + check_skills_index 检查 5·day-review 两处 _shot_check.js 失效指针修复）；2026-08-25（v2.3.103 W504 存量核验状态基线 + 学术轨 105 篇引文核验 A+ 路径：611 篇核验状态·105 绿标·411 引文 100%·D8 修复·content-trust-report 产出）；2026-08-24（v2.3.102 W503 原著引文硬验证：第 20 门禁 check_citations.py 挂载·原文引文行对 text-search.json 精确命中·文档规范 §4.8）；2026-08-24（v2.3.101 W502 术语一致性门禁：第 19 门禁 check_glossary.py 挂载·术语库 6 组 59 条类型化·称谓组规范词锚定·基线豁免 303 篇 383 条）；2026-08-24（v2.3.100 W501 元信息块 v2：第 18 门禁 check_frontmatter.py 挂载·新文件血缘+核验状态 4 字段必填·基线豁免存量 611 篇·文档规范 §4.6 + character-content v2 模板）；2026-08-24（v2.3.98 W499 GitHub 协作模板 + 创意方法论沉淀：.github issue/PR 模板 5 个 + 创意方法论 2 篇 + character-content 创意方法引用 + 方法论 README 索引修复 + file-index 结构注记）；2026-08-24（v2.3.97 W498 防漂移门禁：第 16 门禁转正 + _sync_skills 同步工具 + 18 skill 全量部署）；2026-08-22（v2.3.93 W494 遗留收尾）；2026-08-22（v2.3.92 W493 Phase E6：三门禁转正收官）；2026-08-22（v2.3.91 W492 Phase E5：导航抽屉）；2026-08-22（v2.3.90 W491 Phase E4：EN 85 页对齐）；2026-08-22（v2.3.89 W490 Phase E3：30 页令牌化收官）；2026-08-22（v2.3.88 W489 全站暗色模式：dark 令牌全局化 + 225 页传播 + theme-init）；2026-08-22（v2.3.87 W488 根页视觉重设计 + 夜读模式：6 根页可感知升级 + 暗色提前 + M-A1 前后对比验收）；2026-08-19（v2.3.86 W487 四会话 skill 二轮同步：DRL 降级声明 + experience-capture 格式规范 + 触发词扩充）；2026-08-19（v2.3.85 W486 四会话 skill 协议同步：verdict 7 词 / R0 4 件套 / 警报增强 / P2 残留 / work-log 路径 / 整合版协调）；2026-08-19（v2.3.84 W485 收录三项目 playbook：visual-batch / plan-authoring / plan-review）；2026-08-19（v2.3.83 W484 Skills 目录治理：14 个 skill 审查修复 + 平台适配 + 六文档同步）；2026-08-18（v2.3.82 W464：观测基线快照 + 性能实测 + GoatCounter 链路核验·UV 待后台回填）；2026-08-18（v2.3.81 W478 Phase E2：56 页全量令牌化 + 批次记录 + 门禁全绿）；2026-08-18（v2.3.80 W477 Phase E1 组件层 v2 + 根页模板化：system.css v2 全站传播 + 根页首批 + Noto Sans 子集化 + text-search focus 缺陷修复 + 门禁全绿）；2026-08-18（v2.3.79 W476 Phase E0 纸感轻立体宪改 + tokens v3：三问已决 + E0 探针报告 + DESIGN.md §4A 新立 + tokens.css v3（+2035B 预算内）+ inline_css 225 页同步 + 门禁全绿）；2026-08-17（v2.3.74 W459 V2 审查收尾：D2 回目跳转死链修复（A1_DOC_MAP 100 条映射）+ check_dynamic_links.py 动态链接门禁 + V2 方案文档回写 + 首页 geo-3d 入口 + W456-W458 基线提交）；2026-08-16（v2.3.68 W453 移除评论.txt：结论已沉淀于 CHANGELOG）；2026-08-16（v2.3.67 W452 学术研究显式引用补齐：105 篇 > 引用 + verify 门禁）；2026-08-16（v2.3.66 W451 学术研究引用审计：105 保留 · 9 降教学讲解）；2026-08-16（v2.3.65 W450 统计口径统一与轨标体系：统计口径说明 + 首页精选必看 + 33 篇跨界趣谈 + 纯AI输出披露）；2026-08-16（v2.3.64 W449 冗余文档清理：git rm 删除项目概览/项目认知总览/项目交接参考手册 + 依赖清理 + verify 全绿）；2026-08-16（v2.3.63 W448 外部锐评回应治理：STRUCTURE 版本里程碑归档 110→43KB + README 版本号语义说明 + LICENSE-CONTENT AI 生成方式披露）；2026-08-14（v2.3.40 W425 GoatCounter 真实跨访客统计接入：全站 160 页注入 + CSP 白名单 + _headers 同步）；2026-08-13（v2.3.39 W424 复盘沉淀批次：SRI/CSP 闭环·腐蚀/插件引用门禁·方法论新增·内容同步）；2026-08-12（v2.3.39 W424 对抗性审查修正与全仓整理：A4 门禁校准 209·3D/EN 页修复·security 门禁修复·CI 文档同步·产物清理；v2.3.38 W423 性能债专项：LHCI 预算收紧 + 渲染阻塞消除；v2.3.37 W422 全量治理：P1-P3 优化；v2.3.36 W421 Screenshot Review 提速优化：改动范围判定 + 定向截图 + 浏览器缓存；v2.3.35 W420 A1 内容质量深化：深度解读 100/100 补全 + 56 回元数据补齐 + 99 回导航错链修复；v2.3.26 W411 安全审计 P0-1/P1-1 处置：Web Agent 鉴权加固 + MCP 路径白名单；v2.3.25 W410 npm 依赖审计补充：agent-web 纳入 CI audit + 依赖链修复；v2.3.24 W409 文档同步刷新：交接文档内容纠偏（HEAD 引用/待办1/文件尾最后更新）+ 五文档版本叙述校准；v2.3.23 W408 修 static 资源路径（P2 续）+ v2.3.22 W407 修数据路径代码异味（P2）+ v2.3.21 W406 截图审查纳入发布流程·六文档同步后）。